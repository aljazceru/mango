# Conventions

## Git

- **Never stage or commit wholesale** (`git add -A`, `git add .`). Stage
  specific files for every commit — untracked working notes and local
  artifacts must never ride along accidentally.
