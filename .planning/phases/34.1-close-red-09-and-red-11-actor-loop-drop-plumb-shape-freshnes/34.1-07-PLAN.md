---
phase: 34.1
plan: 07
type: execute
wave: 5
depends_on: ["34.1-01", "34.1-02", "34.1-03", "34.1-04", "34.1-05", "34.1-06"]
files_modified:
  - rust/src/lib.rs
  - .planning/phases/34-integrate-redpill-api-redpill-ai-as-tee-attested-llm-aggrega/34-SUMMARY.md
  - .planning/STATE.md
autonomous: true
requirements: []
requirements_addressed: []
must_haves:
  truths:
    - "The 'deferred to future cache columns' comment is gone from rust/src/lib.rs"
    - "Phase 34 SUMMARY.md notes that RED-09 and RED-11 are now FULL via Phase 34.1"
    - "STATE.md reflects Phase 34.1 completion"
  artifacts:
    - path: ".planning/phases/34-integrate-redpill-api-redpill-ai-as-tee-attested-llm-aggrega/34-SUMMARY.md"
      provides: "Follow-up note crediting Phase 34.1 with closing RED-09/RED-11"
      contains: "Phase 34.1"
    - path: ".planning/STATE.md"
      provides: "Updated current position reflecting Phase 34.1 done"
  key_links: []
---

<objective>
Final cleanup. Confirm the stale "deferred to future cache columns" comment was actually removed from rust/src/lib.rs (already done in 34.1-01 Task 2 — this plan double-checks). Add a follow-up note to Phase 34's SUMMARY.md crediting Phase 34.1 with closing the two PARTIAL must-haves. Update STATE.md to reflect completion.

Purpose: Ensures no stale documentation remains and the project's progress tracking reflects RED-09/RED-11 as FULL.
Output: Verified comment absence, updated Phase 34 SUMMARY note, updated STATE.md current position.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/34-integrate-redpill-api-redpill-ai-as-tee-attested-llm-aggrega/34-SUMMARY.md
@.planning/phases/34-integrate-redpill-api-redpill-ai-as-tee-attested-llm-aggrega/34-VERIFICATION.md
@.planning/phases/34.1-close-red-09-and-red-11-actor-loop-drop-plumb-shape-freshnes/34.1-01-SUMMARY.md
@.planning/phases/34.1-close-red-09-and-red-11-actor-loop-drop-plumb-shape-freshnes/34.1-06-SUMMARY.md

@rust/src/lib.rs
</context>

<tasks>

<task type="auto">
  <name>Task 1: Confirm stale comment is gone from rust/src/lib.rs and verify full test suite</name>
  <files>rust/src/lib.rs</files>
  <read_first>
    - rust/src/lib.rs (lines 7400-7510 — the area where the "deferred to future cache columns" comment used to be)
  </read_first>
  <action>
    Run two greps:
    1. `grep -n "deferred to future cache columns" rust/src/lib.rs` — must return ZERO hits (confirms 34.1-01 Task 2 deletion is intact).
    2. `grep -n "shape: _\|freshness: _\|orchestrated_components: _" rust/src/lib.rs` — must return ZERO hits (confirms no `_` discards remain).

    If either grep returns hits (i.e. the comment or `_` discards reappeared during a merge or revert):
    - Remove the comment block manually.
    - Re-route through `attestation::map_event_to_record_and_status` (the helper from 34.1-01).
    - Re-run the unit tests from 34.1-01 to confirm threading still works.

    Then run the full Rust suite to confirm no regression from any earlier plan:
    ```bash
    cargo test -p mango_core --lib
    ```

    Verify all of the following are still green (these were the in-scope additions from prior plans):
    - `actor_loop_threads_redpill_fields` (34.1-01)
    - `attestation_status_verified_carries_redpill_fields` (34.1-02)
    - `test_migration_v19_adds_three_columns` (34.1-03)
    - `attestation_cache_v19_round_trip` (34.1-03)
    - `cache_pre_v19_row_returns_none_subfields` (34.1-03)

    Plus existing Phase 34 tests:
    - `redpill_orchestrated_event_carries_three_components`
    - `redpill_chutes_shape_carries_per_enclave_freshness`
    - `shape_b_three_way_and_gates_session_open`
  </action>
  <verify>
    <automated>! grep -n "deferred to future cache columns" rust/src/lib.rs && ! grep -nE "shape: _|freshness: _|orchestrated_components: _" rust/src/lib.rs && cargo test -p mango_core --lib 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "deferred to future cache columns" rust/src/lib.rs` returns ZERO hits.
    - `grep -nE "shape: _|freshness: _|orchestrated_components: _" rust/src/lib.rs` returns ZERO hits.
    - `cargo test -p mango_core --lib` exits 0.
    - All five new tests listed above pass.
    - All three existing Phase 34 tests pass.
  </acceptance_criteria>
  <done>No stale comment, no `_` field discards; full Rust lib suite green.</done>
</task>

<task type="auto">
  <name>Task 2: Add follow-up note to Phase 34 SUMMARY.md and update STATE.md</name>
  <files>.planning/phases/34-integrate-redpill-api-redpill-ai-as-tee-attested-llm-aggrega/34-SUMMARY.md, .planning/STATE.md</files>
  <read_first>
    - .planning/phases/34-integrate-redpill-api-redpill-ai-as-tee-attested-llm-aggrega/34-SUMMARY.md (full file)
    - .planning/phases/34-integrate-redpill-api-redpill-ai-as-tee-attested-llm-aggrega/34-VERIFICATION.md (the gap statement that flagged RED-09/RED-11 as PARTIAL)
    - .planning/STATE.md (full file; especially `status:`, `stopped_at:`, `last_activity:`, the `progress.completed_phases` and `progress.completed_plans` counters, and the `Current Position` section). **Capture the exact current values** of `completed_phases` and `completed_plans` from the freshly read file — they are the basis for the arithmetic increment in this task.
  </read_first>
  <action>
    Step 1 — Append a follow-up note to `.planning/phases/34-integrate-redpill-api-redpill-ai-as-tee-attested-llm-aggrega/34-SUMMARY.md` (append to the end of the existing file; do NOT rewrite earlier sections):

    ```markdown
    ## Follow-up: Phase 34.1 closure (2026-04-27)

    Phase 34's verifier audit (34-VERIFICATION.md) flagged RED-09 (PerEnclave freshness sub-line) and RED-11 (Orchestrated three-quote breakdown) as PARTIAL — the cryptographic verification was complete and tested, but the actor-loop destructure at `rust/src/lib.rs:7421-7430` discarded `shape`, `freshness`, and `orchestrated_components` with `_` binds and a "deferred to future cache columns" comment.

    Phase 34.1 closes that drop:
    - Helper `attestation::map_event_to_record_and_status` extracted; actor handler reduced to a single call (34.1-01).
    - `AttestationStatus::Verified` promoted to a struct variant carrying optional `shape`, `freshness`, and `Vec<OrchestratedComponent>` across the UniFFI boundary; Kotlin and Swift bindings regenerated (34.1-02).
    - SQLite migration V19 adds three nullable TEXT columns to `attestation_cache`; round-trip preserves the values; pre-V19 rows hydrate to None (34.1-03).
    - Native trust UI on Android, iOS, and Desktop iced now renders "Verified for this enclave instance" sub-line on PerEnclave and "gateway ✓ • model ✓ • compose ✓" sub-line on Orchestrated, per UI-SPEC.md locked copy (34.1-04, 34.1-05, 34.1-06).

    **RED-09 and RED-11 are FULL as of Phase 34.1.**
    ```

    Step 2 — Update `.planning/STATE.md`:
    - Change frontmatter `status:` from `verifying` to `complete` (or whatever the post-verifier status convention is in this repo — check git log for prior phase completion patterns).
    - Update `stopped_at:` to reflect Phase 34.1 completion: `Phase 34.1 closed RED-09/RED-11 actor-loop drop; UI sub-lines live on all 3 platforms`.
    - Update `last_updated:` to today's ISO timestamp (`"2026-04-27T..."`).
    - Update `last_activity:` to `2026-04-27 — Phase 34.1 plans 34.1-01..34.1-07 complete`.
    - Update `progress.completed_phases` to (current value read in `<read_first>` + 1) and `progress.completed_plans` to (current value read in `<read_first>` + 7). Compute the new exact integers at execution time from the values just read — DO NOT hard-code numbers in this plan, since other concurrent quick-task work may have advanced the counters between planning and execution. Example arithmetic: if the read shows `completed_phases: 13` and `completed_plans: 52`, the post-edit STATE.md must contain `completed_phases: 14` and `completed_plans: 59`.
    - In the `## Current Position` section, change the `Phase:` line to indicate 34.1 done; update `Status:` to reflect that all Phase 34/34.1 must-haves are FULL.
    - Append a bullet to `### Decisions` accumulated context section:
      `- [Phase 34.1]: AttestationStatus::Verified promoted to struct variant carrying shape/freshness/orchestrated_components; SQLite V19 persists them; native UI renders sub-lines under locked UI-SPEC copy on all 3 platforms`
    - In `### Roadmap Evolution`, change the `Phase 34.1 inserted...` line to `Phase 34.1 completed: closed RED-09/RED-11 actor-loop drop ...` (preserve the original entry context, just mark completion).

    Read the existing STATE.md sections carefully before editing — preserve all unrelated content (Quick Tasks Completed, Performance Metrics tables, etc.).
  </action>
  <verify>
    <automated>grep -F "Phase 34.1" .planning/phases/34-integrate-redpill-api-redpill-ai-as-tee-attested-llm-aggrega/34-SUMMARY.md && grep -F "RED-09 and RED-11 are FULL" .planning/phases/34-integrate-redpill-api-redpill-ai-as-tee-attested-llm-aggrega/34-SUMMARY.md && grep -F "Phase 34.1" .planning/STATE.md</automated>
  </verify>
  <acceptance_criteria>
    - `grep -F "RED-09 and RED-11 are FULL" .planning/phases/34-integrate-redpill-api-redpill-ai-as-tee-attested-llm-aggrega/34-SUMMARY.md` returns one hit.
    - `grep -F "Phase 34.1" .planning/phases/34-integrate-redpill-api-redpill-ai-as-tee-attested-llm-aggrega/34-SUMMARY.md` returns at least one hit (the new section).
    - `grep -F "Phase 34.1" .planning/STATE.md` returns at least 2 hits (existing roadmap-evolution entry updated + new decisions bullet).
    - STATE.md frontmatter `last_updated:` is set to today (`2026-04-27`).
    - `progress.completed_plans` increased by exactly 7 versus the value captured in `<read_first>` (the 7 plans of Phase 34.1). Verify with: read the captured pre-edit value `P`, then `grep -E "^  completed_plans: $((P+7))$" .planning/STATE.md` returns exactly one hit AND `grep -E "^  completed_plans: $P$" .planning/STATE.md` returns ZERO hits.
    - `progress.completed_phases` increased by exactly 1 versus the value captured in `<read_first>`. Verify with: read the captured pre-edit value `F`, then `grep -E "^  completed_phases: $((F+1))$" .planning/STATE.md` returns exactly one hit AND `grep -E "^  completed_phases: $F$" .planning/STATE.md` returns ZERO hits.
    - The Phase 34 SUMMARY's earlier sections are preserved (no destructive rewrite — only an appended follow-up section).
  </acceptance_criteria>
  <done>Phase 34 SUMMARY notes the 34.1 closure; STATE.md reflects completion with updated decisions and progress counters.</done>
</task>

</tasks>

<verification>
- All greps in both tasks pass.
- `cargo test -p mango_core --lib` exits 0.
- Phase 34 SUMMARY.md and STATE.md are committed alongside this plan's other artifacts.
</verification>

<success_criteria>
1. No stale "deferred" comment or `_` field discards in rust/src/lib.rs.
2. Full Rust lib test suite green.
3. Phase 34 SUMMARY.md credits Phase 34.1 with closing RED-09 and RED-11.
4. STATE.md reflects Phase 34.1 completion.
</success_criteria>

<output>
After completion, create `.planning/phases/34.1-close-red-09-and-red-11-actor-loop-drop-plumb-shape-freshnes/34.1-07-SUMMARY.md`
</output>
