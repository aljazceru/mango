---
phase: 34.1
plan: 04
type: execute
wave: 4
depends_on: ["34.1-02", "34.1-03"]
files_modified:
  - android/app/src/main/java/dev/disobey/mango/ui/SettingsProvidersScreen.kt
  - android/app/src/main/java/dev/disobey/mango/ui/AttestationBadge.kt
  - android/app/src/main/java/dev/disobey/mango/ui/ChatScreen.kt
  - android/app/src/main/java/dev/disobey/mango/ui/OnboardingScreen.kt
autonomous: true
requirements: ["RED-09", "RED-11"]
requirements_addressed: ["RED-09", "RED-11"]
must_haves:
  truths:
    - "Android Settings → Providers screen renders 'Verified for this enclave instance' sub-line when status is Verified with freshness == 'PerEnclave'"
    - "Android Settings → Providers screen renders 'gateway ✓ • model ✓ • compose ✓' sub-line when status is Verified with non-empty orchestrated_components"
    - "compose_manager component label maps to 'compose'; unknown labels render verbatim"
    - "All other Kotlin AttestationStatus.Verified pattern-match sites compile against the new struct-variant binding"
  artifacts:
    - path: "android/app/src/main/java/dev/disobey/mango/ui/SettingsProvidersScreen.kt"
      provides: "Two new conditional Text() composables under the Verified pill"
      contains: "Verified for this enclave instance"
  key_links:
    - from: "AttestationStatus.Verified (Kotlin binding)"
      to: "ProviderStatusPill / row column composable"
      via: "smart-cast destructure in expanded Column"
      pattern: "is AttestationStatus.Verified"
---

<objective>
Render the two trust-UI sub-lines on the Android Settings → Providers screen per UI-SPEC.md (LOCKED copy). Sub-lines appear ONLY in the expanded provider row, beneath the existing `ProviderStatusPill`. Update other Kotlin pattern-match sites mechanically (compile-fix sweep — no behavior change in those screens).

Purpose: Closes RED-09 (PerEnclave freshness sub-line) and RED-11 (Orchestrated three-quote breakdown) on Android.
Output: Two new conditional Text composables in SettingsProvidersScreen.kt; compile-fix updates in three other Kotlin files; gradle assembleDebug succeeds.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/34.1-close-red-09-and-red-11-actor-loop-drop-plumb-shape-freshnes/34.1-CONTEXT.md
@.planning/phases/34.1-close-red-09-and-red-11-actor-loop-drop-plumb-shape-freshnes/34.1-RESEARCH.md
@.planning/phases/34.1-close-red-09-and-red-11-actor-loop-drop-plumb-shape-freshnes/34.1-PATTERNS.md
@.planning/phases/34.1-close-red-09-and-red-11-actor-loop-drop-plumb-shape-freshnes/34.1-UI-SPEC.md
@.planning/phases/34.1-close-red-09-and-red-11-actor-loop-drop-plumb-shape-freshnes/34.1-02-SUMMARY.md
@.planning/phases/34.1-close-red-09-and-red-11-actor-loop-drop-plumb-shape-freshnes/34.1-03-SUMMARY.md

@android/app/src/main/java/dev/disobey/mango/ui/SettingsProvidersScreen.kt
@android/app/src/main/java/dev/disobey/mango/ui/AttestationBadge.kt
@android/app/src/main/java/dev/disobey/mango/ui/ChatScreen.kt
@android/app/src/main/java/dev/disobey/mango/ui/OnboardingScreen.kt

<interfaces>
LOCKED copy (UI-SPEC.md §"Copywriting Contract"):
- Freshness sub-line: `"Verified for this enclave instance"` (when freshness == "PerEnclave" only)
- Orchestrated breakdown: `"gateway ✓ • model ✓ • compose ✓"` (when orchestrated_components non-empty)
- Component label map: `"gateway" → "gateway"`, `"model" → "model"`, `"compose_manager" → "compose"`. Unknown labels render verbatim.
- Separator: `" • "` (space U+2022 space). Glyph: `" ✓"` (space U+2713).

Typography (UI-SPEC.md §"Typography"):
- Style: `MaterialTheme.typography.labelSmall`
- Color: `MaterialTheme.colorScheme.onSurfaceVariant`
- Spacer between stacked sub-lines: `Spacer(Modifier.height(4.dp))` (xs token)

Insertion point (PATTERNS.md §"android/.../ui/SettingsProvidersScreen.kt"):
- Inside the expanded `Column` in the provider Card, AFTER the Row containing the health pill + attestation label (around lines 187-195 in current tree).
- BEFORE the `backend.models.take(3).joinToString(" · ")` Text (around line 197-203).

Render conditions (UI-SPEC.md §"Interaction & State Contract"):
- Sub-lines render ONLY when `att?.status` is `AttestationStatus.Verified`.
- Sub-lines render ONLY in the expanded row (inside `AnimatedVisibility(visible = isOpen)` or equivalent).
- Sub-lines do NOT render in collapsed pill, chat top-bar, onboarding, or model picker.

Kotlin destructure pattern (post-bindgen — verify shape from regenerated 34.1-02 bindings):
After 34.1-02, `AttestationStatus.Verified` becomes a sealed class / data class with three properties: `shape: String?`, `freshness: String?`, `orchestratedComponents: List<OrchestratedComponent>?`. Use smart-cast: `(status as? AttestationStatus.Verified)?.let { v -> ... }`.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Compile-fix sweep for AttestationStatus.Verified pattern matches in non-providers screens</name>
  <files>android/app/src/main/java/dev/disobey/mango/ui/AttestationBadge.kt, android/app/src/main/java/dev/disobey/mango/ui/ChatScreen.kt, android/app/src/main/java/dev/disobey/mango/ui/OnboardingScreen.kt</files>
  <read_first>
    - android/app/src/main/java/dev/disobey/mango/ui/AttestationBadge.kt (lines 64-145 — the four-variant when block)
    - android/app/src/main/java/dev/disobey/mango/ui/ChatScreen.kt (lines 415, 454-458 — pattern-match for color/icon)
    - android/app/src/main/java/dev/disobey/mango/ui/OnboardingScreen.kt (lines 394-506 — Verified pattern-match)
    - The regenerated Kotlin bindings under android/app/src/main/java/dev/disobey/mango/rust/ (the AttestationStatus sealed class shape after 34.1-02)
    - .planning/phases/34.1-close-red-09-and-red-11-actor-loop-drop-plumb-shape-freshnes/34.1-PATTERNS.md (section "Compile-driven sweep")
  </read_first>
  <action>
    Run `cd android && ./gradlew assembleDebug 2>&1 | tee /tmp/34.1-04-task1.log` and inspect compile errors related to `AttestationStatus.Verified`.

    For each error, apply the rule:
    - `is AttestationStatus.Verified ->` (when used WITHOUT consulting fields) — works as-is in Kotlin (smart-cast on sealed class with `is` check is unaffected by data-class properties). Only update if the compiler complains.
    - `AttestationStatus.Verified ->` (Kotlin `when` with object reference, NOT `is`) — if the binding makes Verified a data class (not an object), Kotlin won't allow this; change to `is AttestationStatus.Verified ->`.
    - For destructure-style references (e.g. `case .verified` style in any Kotlin code, which is rare), use smart-cast: `is AttestationStatus.Verified -> { val v = it; ... }`.

    Specifically:
    - `AttestationBadge.kt`: the four-variant when on `AttestationStatus` — change `AttestationStatus.Verified ->` to `is AttestationStatus.Verified ->` if needed. Behavior: continues to render the existing single-line Verified badge — no sub-line additions in this file (per CONTEXT.md: badge stays single-line).
    - `ChatScreen.kt:415, 454-458`: same compile-fix; behavior unchanged (color/icon only).
    - `OnboardingScreen.kt:394-506`: same compile-fix; behavior unchanged (Verified treated as bare verified, no sub-fields displayed).

    DO NOT add any sub-line rendering in these three files. They are explicitly out of scope per UI-SPEC.md §"Visibility": sub-lines render ONLY on Settings → Providers.

    Iterate `./gradlew assembleDebug` until clean for these three files (the SettingsProvidersScreen.kt compile errors are addressed in Task 2).
  </action>
  <verify>
    <automated>cd android && ./gradlew compileDebugKotlin 2>&1 | tee /tmp/34.1-04-task1-verify.log | tail -30; ! grep -E "AttestationBadge\.kt.*error:|ChatScreen\.kt.*error:|OnboardingScreen\.kt.*error:" /tmp/34.1-04-task1-verify.log</automated>
  </verify>
  <acceptance_criteria>
    - `cd android && ./gradlew compileDebugKotlin` produces ZERO errors in AttestationBadge.kt, ChatScreen.kt, OnboardingScreen.kt (errors in SettingsProvidersScreen.kt are acceptable here — Task 2 fixes them).
    - `grep -n "AttestationStatus.Verified" android/app/src/main/java/dev/disobey/mango/ui/AttestationBadge.kt` shows the existing four cases unchanged in semantics; the badge still renders single-line per CONTEXT.md.
    - No new sub-line Text() composables introduced in these three files.
  </acceptance_criteria>
  <done>Three non-providers Kotlin files compile against the new struct-variant Verified binding; behavior in chat / onboarding / badge widget unchanged.</done>
</task>

<task type="auto">
  <name>Task 2: Render freshness + orchestrated breakdown sub-lines in SettingsProvidersScreen.kt</name>
  <files>android/app/src/main/java/dev/disobey/mango/ui/SettingsProvidersScreen.kt</files>
  <read_first>
    - android/app/src/main/java/dev/disobey/mango/ui/SettingsProvidersScreen.kt (lines 100-270; especially the expanded provider Card column at lines 161-203 and the attestation read at lines 187-195)
    - .planning/phases/34.1-close-red-09-and-red-11-actor-loop-drop-plumb-shape-freshnes/34.1-UI-SPEC.md (full file — copy is LOCKED verbatim)
    - .planning/phases/34.1-close-red-09-and-red-11-actor-loop-drop-plumb-shape-freshnes/34.1-PATTERNS.md (section "android/.../ui/SettingsProvidersScreen.kt")
    - The regenerated AttestationStatus binding under android/app/src/main/java/dev/disobey/mango/rust/
  </read_first>
  <action>
    Inside the expanded Column of the provider Card, AFTER the Row that renders the health pill + attestation-style label (around line 195) and BEFORE the `backend.models.take(3)` block (around line 197-203), insert the sub-line block.

    Use this EXACT structure (adapt smart-cast / property names if the Kotlin binding produces slightly different camelCase — verify by reading the regenerated file first):

    ```kotlin
    // Phase 34.1: trust-UI sub-lines for Redpill freshness + orchestrated breakdown.
    // Copy LOCKED in 34.1-UI-SPEC.md. Sub-lines render only on Settings → Providers
    // expanded row, only when status is Verified with the relevant fields populated.
    val verified = att?.status as? AttestationStatus.Verified
    if (verified != null) {
        if (verified.freshness == "PerEnclave") {
            Spacer(Modifier.height(4.dp))
            Text(
                "Verified for this enclave instance",
                style = MaterialTheme.typography.labelSmall,
                color = MaterialTheme.colorScheme.onSurfaceVariant,
            )
        }
        val comps = verified.orchestratedComponents
        if (!comps.isNullOrEmpty()) {
            val labelMap = mapOf(
                "gateway" to "gateway",
                "model" to "model",
                "compose_manager" to "compose",
            )
            val line = comps.joinToString(separator = " • ") { c ->
                "${labelMap[c.label] ?: c.label} ✓"
            }
            Spacer(Modifier.height(4.dp))
            Text(
                line,
                style = MaterialTheme.typography.labelSmall,
                color = MaterialTheme.colorScheme.onSurfaceVariant,
            )
        }
    }
    ```

    Verify property names against the regenerated binding:
    - If `OrchestratedComponent` Kotlin record exposes `label: String, value: String` directly, use `c.label` (as above).
    - If the camelCase converter produced `orchestratedComponents` for the `orchestrated_components` field, use that (Kotlin convention — UniFFI's default).

    Imports to add at top of file if not already present:
    - `import androidx.compose.foundation.layout.Spacer`
    - `import androidx.compose.foundation.layout.height`
    - `import androidx.compose.material3.MaterialTheme`
    - `import androidx.compose.material3.Text`
    - `import androidx.compose.ui.Modifier`
    - `import androidx.compose.ui.unit.dp`
    - `import dev.disobey.mango.rust.AttestationStatus`

    Most of these likely already exist — verify before adding to avoid duplicate-import warnings.

    DO NOT modify ProviderStatusPill (the badge stays single-line per CONTEXT.md).
    DO NOT change `attestationStyleProviders` mapping (existing four-variant function — only update its `is AttestationStatus.Verified ->` case to bind `{ .. }`-style if compiler requires).
  </action>
  <verify>
    <automated>cd android && ./gradlew assembleDebug 2>&1 | tee /tmp/34.1-04-task2.log | tail -30; ! grep -E "error:" /tmp/34.1-04-task2.log; grep -c "Verified for this enclave instance" android/app/src/main/java/dev/disobey/mango/ui/SettingsProvidersScreen.kt</automated>
  </verify>
  <acceptance_criteria>
    - `grep -F "Verified for this enclave instance" android/app/src/main/java/dev/disobey/mango/ui/SettingsProvidersScreen.kt` returns exactly one hit (the locked copy).
    - `grep -F " ✓" android/app/src/main/java/dev/disobey/mango/ui/SettingsProvidersScreen.kt` returns at least one hit (the per-component success glyph).
    - `grep -F " • " android/app/src/main/java/dev/disobey/mango/ui/SettingsProvidersScreen.kt` returns at least one hit (the separator).
    - `grep -F "compose_manager" android/app/src/main/java/dev/disobey/mango/ui/SettingsProvidersScreen.kt` returns at least one hit (the label map source key).
    - `grep -F "compose" android/app/src/main/java/dev/disobey/mango/ui/SettingsProvidersScreen.kt | grep -v "compose_manager" | grep -v "import "` shows the mapped target value "compose".
    - `grep -F "PerEnclave" android/app/src/main/java/dev/disobey/mango/ui/SettingsProvidersScreen.kt` returns at least one hit (the render condition).
    - `cd android && ./gradlew assembleDebug` exits 0.
    - `grep -F "ProviderStatusPill" android/app/src/main/java/dev/disobey/mango/ui/SettingsProvidersScreen.kt` shows the existing pill is unchanged (the badge stays single-line).
  </acceptance_criteria>
  <done>Two sub-lines render under the Verified pill with locked copy; gradle assembleDebug succeeds; badge unchanged.</done>
</task>

</tasks>

<verification>
- `cd android && ./gradlew assembleDebug` exits 0 (full Android app builds).
- The four enumerated greps above pass.
- Manual-only smoke (deferred to Phase verification): build debug APK, add Redpill provider with Chutes model → freshness sub-line shows; switch to Orchestrated model → breakdown sub-line shows.
</verification>

<success_criteria>
1. Android compileDebugKotlin succeeds end-to-end.
2. SettingsProvidersScreen.kt renders both sub-lines under the locked render conditions with the locked copy.
3. AttestationBadge / ChatScreen / OnboardingScreen behavior unchanged (compile-fix only).
4. compose_manager → compose mapping applied; unknown labels fallback to verbatim.
</success_criteria>

<output>
After completion, create `.planning/phases/34.1-close-red-09-and-red-11-actor-loop-drop-plumb-shape-freshnes/34.1-04-SUMMARY.md`
</output>
