---
phase: 34.1
plan: 06
type: execute
wave: 4
depends_on: ["34.1-02", "34.1-03"]
files_modified:
  - desktop/iced/src/views/settings_providers.rs
  - desktop/iced/src/widgets/attestation_badge.rs
  - desktop/iced/src/views/chat.rs
  - desktop/iced/src/views/onboarding.rs
autonomous: true
requirements: ["RED-09", "RED-11"]
requirements_addressed: ["RED-09", "RED-11"]
must_haves:
  truths:
    - "Desktop iced Settings → Providers view renders 'Verified for this enclave instance' sub-line when status is Verified with freshness == 'PerEnclave'"
    - "Desktop iced Settings → Providers view renders 'gateway ✓ • model ✓ • compose ✓' sub-line when status is Verified with non-empty orchestrated_components"
    - "All other iced AttestationStatus::Verified pattern-match sites compile against the new struct variant"
  artifacts:
    - path: "desktop/iced/src/views/settings_providers.rs"
      provides: "Two new conditional text() sub-line widgets in the row column"
      contains: "Verified for this enclave instance"
  key_links:
    - from: "AttestationStatus::Verified { .. } (Rust)"
      to: "settings_providers row column!"
      via: "match arm with field bindings + push text(...)"
      pattern: "AttestationStatus::Verified { freshness, orchestrated_components"
---

<objective>
Mirror the Android/iOS sub-line rendering on Desktop iced per UI-SPEC.md (LOCKED copy). Update every iced `AttestationStatus::Verified` match site to the new struct variant (compile-fix sweep — no behavior change in chat / onboarding / badge widget).

Purpose: Closes RED-09 and RED-11 on Desktop.
Output: Two new conditional `text(...)` sub-lines in settings_providers.rs; compile-fix updates in three other iced files; `cargo build` for the desktop crate succeeds.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/34.1-close-red-09-and-red-11-actor-loop-drop-plumb-shape-freshnes/34.1-CONTEXT.md
@.planning/phases/34.1-close-red-09-and-red-11-actor-loop-drop-plumb-shape-freshnes/34.1-RESEARCH.md
@.planning/phases/34.1-close-red-09-and-red-11-actor-loop-drop-plumb-shape-freshnes/34.1-PATTERNS.md
@.planning/phases/34.1-close-red-09-and-red-11-actor-loop-drop-plumb-shape-freshnes/34.1-UI-SPEC.md
@.planning/phases/34.1-close-red-09-and-red-11-actor-loop-drop-plumb-shape-freshnes/34.1-02-SUMMARY.md

@desktop/iced/src/views/settings_providers.rs
@desktop/iced/src/widgets/attestation_badge.rs
@desktop/iced/src/views/chat.rs
@desktop/iced/src/views/onboarding.rs

<interfaces>
LOCKED copy (UI-SPEC.md):
- Freshness sub-line: "Verified for this enclave instance" (only when freshness == "PerEnclave")
- Orchestrated breakdown: "gateway ✓ • model ✓ • compose ✓"
- Component label map: compose_manager → compose; unknown verbatim
- Separator: " • "; glyph: " ✓"

Typography (UI-SPEC.md §"Typography" — Desktop iced row):
- `text(...).size(11)` (matches existing `attest_label` patterns at settings_providers.rs:50-62)
- Color: `vc.muted` (existing iced theme color used by `attest_label`)

Insertion point (PATTERNS.md §"desktop/iced/src/views/settings_providers.rs"):
- Inside the row builder column!, AFTER the `attest_label` text is pushed.
- Use existing `column.push(text("...").size(11).color(vc.muted))` patterns.

Rust pattern: AttestationStatus::Verified is a struct variant after 34.1-02. Match form: `AttestationStatus::Verified { freshness, orchestrated_components, .. }` to bind only what's used. Wildcard `..` covers `shape` (not consumed by UI logic).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Compile-fix sweep for AttestationStatus::Verified in non-providers iced files</name>
  <files>desktop/iced/src/widgets/attestation_badge.rs, desktop/iced/src/views/chat.rs, desktop/iced/src/views/onboarding.rs</files>
  <read_first>
    - desktop/iced/src/widgets/attestation_badge.rs (lines 14-65 — four-variant match)
    - desktop/iced/src/views/chat.rs (lines 150-155)
    - desktop/iced/src/views/onboarding.rs (lines 395, 506)
  </read_first>
  <action>
    For each match arm of `AttestationStatus::Verified` that does NOT consume the new fields (none of these three files do per CONTEXT.md scope), change `AttestationStatus::Verified =>` to `AttestationStatus::Verified { .. } =>`.

    Specifically:
    - `widgets/attestation_badge.rs`: badge stays single-line per CONTEXT.md. Update the `Verified =>` arm to `Verified { .. } =>`. Behavior unchanged.
    - `views/chat.rs:150-155`: status used for color/icon. Same compile-fix.
    - `views/onboarding.rs:395, 506`: same compile-fix; no sub-lines surfaced during onboarding.

    DO NOT add any sub-line rendering in these three files (out of scope per UI-SPEC visibility rules).
  </action>
  <verify>
    <automated>cargo check -p mango_iced 2>&1 | tee /tmp/34.1-06-task1.log | tail -30; ! grep -E "attestation_badge\.rs.*error|chat\.rs.*error|onboarding\.rs.*error" /tmp/34.1-06-task1.log</automated>
  </verify>
  <acceptance_criteria>
    - `grep -nE "AttestationStatus::Verified[^{ ]" desktop/iced/src/widgets/attestation_badge.rs desktop/iced/src/views/chat.rs desktop/iced/src/views/onboarding.rs` returns ZERO hits (no bare unit-variant matches remain).
    - `cargo check -p mango_iced` produces ZERO errors in these three files (errors in settings_providers.rs are acceptable here — Task 2 fixes them).
  </acceptance_criteria>
  <done>Three iced files compile against the new struct-variant Verified; behavior unchanged.</done>
</task>

<task type="auto">
  <name>Task 2: Render freshness + orchestrated breakdown sub-lines in settings_providers.rs</name>
  <files>desktop/iced/src/views/settings_providers.rs</files>
  <read_first>
    - desktop/iced/src/views/settings_providers.rs (full file; especially `attest_label` at lines 41-48 and the row builder where it's invoked)
    - .planning/phases/34.1-close-red-09-and-red-11-actor-loop-drop-plumb-shape-freshnes/34.1-UI-SPEC.md
    - .planning/phases/34.1-close-red-09-and-red-11-actor-loop-drop-plumb-shape-freshnes/34.1-PATTERNS.md (section "desktop/iced/src/views/settings_providers.rs")
    - rust/src/attestation/mod.rs (the AttestationStatus + OrchestratedComponent shapes from 34.1-02)
  </read_first>
  <action>
    Step 1 — Update `attest_label` (lines 41-48):
    ```rust
    fn attest_label(s: &AttestationStatus, vc: crate::theme::ViewColors) -> (&'static str, Color) {
        match s {
            AttestationStatus::Verified { .. } => ("Verified", vc.success),
            AttestationStatus::Unverified => ("Unverified", vc.muted),
            AttestationStatus::Failed { .. } => ("Failed", vc.destructive),
            AttestationStatus::Expired => ("Expired", vc.warning),
        }
    }
    ```

    Step 2 — In the row builder (find the place where `attest_label(...)` is invoked and pushed into a `column!` / `Column::new()`), AFTER the existing badge / attest-label text push, insert two conditional sub-lines.

    Locate the builder fn (likely a closure or a `provider_row` helper that takes the AttestationStatus). Read 50 lines around the `attest_label` invocation to identify the column accumulator variable name. The pattern below uses `col` as a placeholder — substitute the actual local variable from the file:

    ```rust
    // Phase 34.1: trust-UI sub-lines for Redpill freshness + orchestrated breakdown.
    // Copy LOCKED in 34.1-UI-SPEC.md.
    if let AttestationStatus::Verified { freshness, orchestrated_components, .. } = &att.status {
        if freshness.as_deref() == Some("PerEnclave") {
            col = col.push(
                text("Verified for this enclave instance")
                    .size(11)
                    .color(vc.muted),
            );
        }
        if let Some(comps) = orchestrated_components {
            if !comps.is_empty() {
                let line = comps
                    .iter()
                    .map(|c| {
                        let mapped = match c.label.as_str() {
                            "gateway" => "gateway",
                            "model" => "model",
                            "compose_manager" => "compose",
                            other => other,
                        };
                        format!("{} ✓", mapped)
                    })
                    .collect::<Vec<_>>()
                    .join(" • ");
                col = col.push(text(line).size(11).color(vc.muted));
            }
        }
    }
    ```

    If the row is built inside `column![]` macro (not a mutable accumulator), refactor to mutable form OR inline the sub-lines using `column!`'s syntax (push items conditionally is awkward in macro form; mutable `Column::new().push(...)` is cleaner). Read the actual file before deciding which form to use.

    Imports to ensure (likely already present):
    - `use mango_core::attestation::{AttestationStatus, OrchestratedComponent};` (or the equivalent `mango_core` re-export path)
    - `use iced::widget::text;`

    DO NOT modify the badge widget (single-line per CONTEXT.md).
    DO NOT change visual styling beyond the locked typography (size 11 + vc.muted).
  </action>
  <verify>
    <automated>cargo build -p mango_iced 2>&1 | tail -20 && grep -F "Verified for this enclave instance" desktop/iced/src/views/settings_providers.rs && grep -F "compose_manager" desktop/iced/src/views/settings_providers.rs && grep -F "PerEnclave" desktop/iced/src/views/settings_providers.rs && grep -F " ✓" desktop/iced/src/views/settings_providers.rs && grep -F " • " desktop/iced/src/views/settings_providers.rs</automated>
  </verify>
  <acceptance_criteria>
    - `grep -F "Verified for this enclave instance" desktop/iced/src/views/settings_providers.rs` returns exactly one hit.
    - `grep -F " ✓" desktop/iced/src/views/settings_providers.rs` returns at least one hit.
    - `grep -F " • " desktop/iced/src/views/settings_providers.rs` returns at least one hit.
    - `grep -F "compose_manager" desktop/iced/src/views/settings_providers.rs` returns at least one hit.
    - `grep -F "PerEnclave" desktop/iced/src/views/settings_providers.rs` returns at least one hit.
    - `grep -nE "AttestationStatus::Verified[^{]" desktop/iced/src/views/settings_providers.rs` returns ZERO hits (no bare unit-variant matches; all are struct-variant form).
    - `cargo build -p mango_iced` succeeds.
  </acceptance_criteria>
  <done>Two sub-lines render in the providers row column with locked copy; desktop iced crate builds cleanly.</done>
</task>

</tasks>

<verification>
- `cargo build -p mango_iced` exits 0.
- All five grep checks pass on settings_providers.rs.
- Manual smoke (deferred to phase verification): launch desktop, navigate to Settings → Providers, observe the two sub-lines.
</verification>

<success_criteria>
1. settings_providers.rs renders both sub-lines under locked render conditions with locked copy.
2. attestation_badge.rs / chat.rs / onboarding.rs compile against the struct variant (compile-fix only).
3. compose_manager → compose mapping applied; unknown labels fallback verbatim.
4. Badge widget remains single-line.
</success_criteria>

<output>
After completion, create `.planning/phases/34.1-close-red-09-and-red-11-actor-loop-drop-plumb-shape-freshnes/34.1-06-SUMMARY.md`
</output>
