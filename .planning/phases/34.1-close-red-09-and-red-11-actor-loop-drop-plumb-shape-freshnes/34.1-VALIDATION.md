---
phase: 34.1
slug: close-red-09-and-red-11-actor-loop-drop-plumb-shape-freshnes
status: draft
nyquist_compliant: false
wave_0_complete: false
created: 2026-04-27
---

# Phase 34.1 — Validation Strategy

> Per-phase validation contract for feedback sampling during execution.
> Closes RED-09 / RED-11 actor-loop drop. Validation here verifies the
> data threading, NOT the cryptographic verifier (locked, untouched).

---

## Test Infrastructure

| Property | Value |
|----------|-------|
| **Framework** | `cargo test` (Rust 2021) + Android instrumentation/unit (existing) + Swift XCTest (existing, where present) |
| **Config file** | `rust/Cargo.toml` (workspace member `mango_core`) |
| **Quick run command** | `cargo test -p mango_core --lib attestation::` |
| **Full suite command** | `cargo test -p mango_core --lib` |
| **Estimated runtime** | ~30-90 seconds (Rust unit suite) |

---

## Sampling Rate

- **After every task commit:** Run `cargo test -p mango_core --lib attestation::` (scoped to attestation module)
- **After every plan wave:** Run `cargo test -p mango_core --lib`
- **Before `/gsd-verify-work`:** Full suite must be green; bindings regenerated and committed
- **Max feedback latency:** 90 seconds

---

## Per-Task Verification Map

> Task IDs are placeholders — final IDs come from PLAN.md output. Update this
> table once plans are written. Each row maps a task to a measurable check.

| Task ID | Plan | Wave | Requirement | Secure Behavior | Test Type | Automated Command | File Exists | Status |
|---------|------|------|-------------|-----------------|-----------|-------------------|-------------|--------|
| 34.1-01-XX | 01 actor-loop wiring | 1 | RED-09, RED-11 | Verified attestation events thread shape/freshness/orchestrated through actor loop without dropping | unit | `cargo test -p mango_core --lib actor_loop_threads_redpill_fields` | ❌ W0 | ⬜ pending |
| 34.1-02-XX | 02 UniFFI variant | 1 | RED-09, RED-11 | `AttestationStatus::Verified` carries optional shape/freshness/orchestrated; bindings regen successfully | unit + bindgen | `cargo test -p mango_core --lib attestation_status_verified_carries_redpill_fields && just bindings-kotlin && just bindings-swift` | ❌ W0 | ⬜ pending |
| 34.1-03-XX | 03 SQLite V19 migration | 1 | RED-09, RED-11 | New columns added; pre-V19 cache rows hydrate to `Verified { shape: None, ... }`; new rows round-trip Some-values | unit | `cargo test -p mango_core --lib attestation_cache_v19_round_trip` | ❌ W0 | ⬜ pending |
| 34.1-04-XX | 04 Android UI | 2 | RED-09, RED-11 | Freshness sub-line renders on PerEnclave; three-component breakdown renders on Orchestrated | UI / runtime check | `./gradlew :app:testDebugUnitTest --tests "*ProvidersScreen*"` (or runtime hand-verify if no test exists) | ❌ W0 | ⬜ pending |
| 34.1-05-XX | 05 iOS UI | 2 | RED-09, RED-11 | Same renderings on iOS SwiftUI providers screen | XCTest / runtime | `xcodebuild test -scheme Mango -only-testing:MangoTests/ProvidersViewTests` (if tests exist; else manual smoke) | ❌ W0 | ⬜ pending |
| 34.1-06-XX | 06 Desktop iced UI | 2 | RED-09, RED-11 | Same renderings in desktop iced providers view | runtime / unit | `cargo test -p desktop` (if tests exist; else manual smoke) | ❌ W0 | ⬜ pending |
| 34.1-07-XX | 07 cleanup | 3 | — | Stale "deferred" comment at lib.rs:7421-7427 removed; Phase 34 SUMMARY referenced | grep | `! grep -n "deferred to future cache columns" rust/src/lib.rs` | ✅ | ⬜ pending |

*Status: ⬜ pending · ✅ green · ❌ red · ⚠️ flaky*

---

## Wave 0 Requirements

Wave 0 installs the unit-test scaffolds for tasks above before implementation begins. Required new test files / additions:

- [ ] `rust/src/tests/redpill.rs` — extend with three new tests:
  - [ ] `actor_loop_threads_redpill_fields` (per shape A/B/C: assert AppState's per-backend status carries the expected shape/freshness/orchestrated_components after `verify_backend_attestation`).
  - [ ] `attestation_status_verified_carries_redpill_fields` (constructs the new struct variant and asserts field access from a UniFFI-shaped consumer).
  - [ ] `attestation_cache_v19_round_trip` (writes a record with Some-values to the SQLite cache, re-reads, asserts equality; second case writes an old V18-shaped row and asserts re-hydration with None).
- [ ] No new test framework needed — `cargo test` already wired.
- [ ] Helper extraction (`map_event_to_record_and_status`) per RESEARCH.md §Approach so tests can exercise threading without Tokio runtime.

*If none: "N/A — see above."*

---

## Manual-Only Verifications

| Behavior | Requirement | Why Manual | Test Instructions |
|----------|-------------|------------|-------------------|
| Android trust UI freshness sub-line | RED-09 | UI snapshot infra not present in repo; visual confirmation only | Build debug APK, add Redpill provider with a Chutes model, observe sub-line "Verified for this enclave instance" under the Verified pill |
| Android orchestrated three-quote breakdown | RED-11 | Same | Same as above with an Orchestrated-shape model; observe "gateway ✓ • model ✓ • compose ✓" sub-row |
| iOS sub-line + breakdown | RED-09, RED-11 | XCTest snapshot infra likely absent | Build iOS sim, repeat Android steps |
| Desktop iced sub-line + breakdown | RED-09, RED-11 | Same | Build desktop, repeat |
| Live E2E (Phase 34's 4 ignored tests still gate the actual SC #10) | SC #10 | Network-bound; gated `#[ignore]` | `cargo test -p mango_core --lib live_redpill -- --ignored --nocapture` (user sign-off, network access required) |

---

## Validation Sign-Off

- [ ] All tasks have `<automated>` verify or Wave 0 dependencies
- [ ] Sampling continuity: no 3 consecutive tasks without automated verify
- [ ] Wave 0 covers all MISSING references
- [ ] No watch-mode flags
- [ ] Feedback latency < 90s
- [ ] `nyquist_compliant: true` set in frontmatter

**Approval:** pending
