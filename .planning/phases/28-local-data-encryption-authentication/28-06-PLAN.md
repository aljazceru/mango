---
phase: 28-local-data-encryption-authentication
plan: 06
type: execute
wave: 3
depends_on: ["28-02"]
files_modified:
  - desktop/iced/src/lock_screen.rs
  - desktop/iced/src/pin_setup_screen.rs
  - desktop/iced/src/main.rs
autonomous: false
requirements:
  - ENC-07
  - ENC-09
  - ENC-12
  - ENC-13
  - ENC-14

must_haves:
  truths:
    - "Desktop app shows lock screen on cold launch requiring PIN to unlock"
    - "First-time users see PIN setup screen with optional duress PIN"
    - "Desktop uses NullBiometricProvider (PIN-only, per D-23)"
    - "macOS Touch ID is opportunistic via keyring crate (no explicit BiometricProvider needed)"
  artifacts:
    - path: "desktop/iced/src/lock_screen.rs"
      provides: "iced lock screen with PIN input"
    - path: "desktop/iced/src/pin_setup_screen.rs"
      provides: "iced PIN setup screen"
  key_links:
    - from: "desktop/iced/src/main.rs"
      to: "Screen::Locked"
      via: "Match on screen enum routes to lock_screen::view"
      pattern: "Screen::Locked"
---

<objective>
Implement Desktop (iced) lock screen and PIN setup screen. Desktop uses PIN-only authentication
(NullBiometricProvider per D-23). macOS Touch ID works opportunistically through keyring crate
when storing/loading the DEK.

Purpose: Desktop platform gets the same encryption and lock gate as mobile platforms.

Output: Working desktop lock/unlock with PIN entry and first-time setup.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/28-local-data-encryption-authentication/28-CONTEXT.md
@.planning/phases/28-local-data-encryption-authentication/28-02-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create desktop lock_screen.rs, pin_setup_screen.rs, and wire routing</name>
  <files>desktop/iced/src/lock_screen.rs, desktop/iced/src/pin_setup_screen.rs, desktop/iced/src/main.rs</files>
  <read_first>
    - desktop/iced/src/main.rs (screen routing, Message enum, App struct, view function)
    - desktop/iced/src/settings.rs (reference for iced UI patterns -- text_input, button, column layout)
  </read_first>
  <action>
    **lock_screen.rs:**
    - iced view function: centered column with app name, secure text_input for PIN (set `is_secure(true)` or use password field pattern from iced), "Unlock" button
    - Message::UnlockSubmit dispatches AppAction::UnlockWithPin
    - No biometric button on desktop (per D-23)
    - Show error text if unlock fails (from AppState.toast)

    **pin_setup_screen.rs:**
    - Step-by-step: PIN field + confirm, optional duress PIN field, no biometric toggle on desktop
    - Message::PinSetupSubmit dispatches AppAction::SetupPin with enable_biometric=false
    - Validation: min 4 chars, PINs match, duress differs from real

    **main.rs routing:**
    - Add `mod lock_screen; mod pin_setup_screen;`
    - Add Message variants: UnlockPinChanged, UnlockSubmit, PinSetupPinChanged, PinSetupConfirmChanged, PinSetupDuressChanged, PinSetupSubmit
    - Add state fields: lock_pin_input, setup_pin_input, setup_confirm_input, setup_duress_input
    - Route Screen::Locked to lock_screen::view, Screen::PinSetup to pin_setup_screen::view
    - Pass NullBiometricProvider to FfiApp::new (desktop already uses NullKeychainProvider pattern -- follow same for biometric)

    Note: Desktop actually uses DesktopKeychainProvider (not Null). The biometric provider is NullBiometricProvider since desktop has no standard biometric API. On macOS, the keyring crate may trigger Touch ID when accessing the keychain entry for the DEK -- this is transparent and requires no code.
  </action>
  <verify>
    <automated>cd /home/lio/g/confidential-app && cargo build -p desktop 2>&1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - File desktop/iced/src/lock_screen.rs exists
    - File desktop/iced/src/pin_setup_screen.rs exists
    - grep -q "Screen::Locked\|Locked =>" desktop/iced/src/main.rs
    - grep -q "Screen::PinSetup\|PinSetup =>" desktop/iced/src/main.rs
    - grep -q "NullBiometricProvider" desktop/iced/src/main.rs
    - cargo build -p desktop succeeds
  </acceptance_criteria>
  <done>Desktop lock screen and PIN setup work with PIN-only auth. macOS Touch ID works transparently via keyring.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>Desktop lock screen with PIN unlock and first-time PIN setup</what-built>
  <how-to-verify>
    1. Run desktop app (clear data_dir first for fresh install)
    2. Verify PIN setup appears, set a PIN
    3. Restart app -- verify lock screen appears
    4. Enter PIN -- verify unlock to home screen
    5. Test wrong PIN -- verify error shown
  </how-to-verify>
  <resume-signal>Type "approved" or describe issues</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Desktop PIN input -> Rust | PIN crosses from iced UI to actor thread |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-28-23 | Information Disclosure | lock_screen PIN field | mitigate | Use iced secure text input (masked); clear input string after submit |
| T-28-24 | Spoofing | Desktop has no biometric | accept | PIN-only is the documented baseline for desktop (per D-23, D-24) |
</threat_model>

<verification>
- `cargo build -p desktop` succeeds
- Desktop app launches to lock screen
</verification>

<success_criteria>
- Desktop lock and PIN setup screens render correctly
- PIN unlock works, wrong PIN shows error
- App state restored after unlock
</success_criteria>

<output>
After completion, create `.planning/phases/28-local-data-encryption-authentication/28-06-SUMMARY.md`
</output>
