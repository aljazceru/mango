---
phase: 31
plan: 02
type: execute
wave: 3
depends_on: ["31-01"]
files_modified:
  - ios/Bindings/mango_core.swift
  - ios/Bindings/mango_coreFFI.h
  - ios/Bindings/mango_coreFFI.modulemap
  - android/app/src/main/java/dev/disobey/mango/rust/mango_core.kt
autonomous: true
requirements:
  - IMG-04
user_setup: []
must_haves:
  truths:
    - "UniFFI bindings for iOS and Android expose AttachImage action and AttachmentInfo.is_image field"
    - "Committed binding files match the current rust/src/lib.rs public surface (no divergence)"
  artifacts:
    - path: ios/Bindings/mango_core.swift
      provides: "Regenerated Swift bindings with AttachImage + isImage"
      contains: "attachImage"
    - path: android/app/src/main/java/dev/disobey/mango/rust/mango_core.kt
      provides: "Regenerated Kotlin bindings with AttachImage + isImage"
      contains: "AttachImage"
  key_links:
    - from: ios/Bindings/mango_core.swift
      to: rust/src/lib.rs AppAction::AttachImage
      via: "UniFFI Swift codegen"
      pattern: "case attachImage"
    - from: android/app/src/main/java/dev/disobey/mango/rust/mango_core.kt
      to: rust/src/lib.rs AppAction::AttachImage
      via: "UniFFI Kotlin codegen"
      pattern: "data class AttachImage"
---

<objective>
Regenerate UniFFI bindings for iOS and Android so the new `AppAction::AttachImage` variant and `AttachmentInfo.is_image` field are visible to SwiftUI and Jetpack Compose code. Commit the regenerated files to the repo (Phase 24 precedent: iOS Bindings/ is committed so Xcode builds without requiring local Rust toolchain).

Purpose: Unblock the three platform plans (31-03 Android, 31-04 iOS) that need to dispatch `AttachImage` from native UI. Desktop (31-05) uses Rust directly via `mango_core` crate — no bindings needed.
Output: Updated `.swift`, `.h`, `.modulemap`, `.kt` files under `ios/Bindings/` and `android/app/src/main/java/dev/disobey/mango/rust/`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/31-multimodal-image-attachments-across-all-platforms-extend-rus/31-01-PLAN.md
@.planning/STATE.md
@justfile
</context>

<tasks>

<task type="auto">
  <name>Task 1: Regenerate iOS + Android bindings, verify diff, commit</name>
  <files>ios/Bindings/mango_core.swift, ios/Bindings/mango_coreFFI.h, ios/Bindings/mango_coreFFI.modulemap, android/app/src/main/java/dev/disobey/mango/rust/mango_core.kt</files>
  <read_first>
    - justfile (look for `bindings-kotlin` and `bindings-swift` recipes)
    - Phase 24-01 SUMMARY (if it exists under .planning/phases/24-redesign-settings-ux/) — same precedent for committing iOS Bindings/
    - `grep -n 'AttachImage\|is_image' ios/Bindings/mango_core.swift android/app/src/main/java/dev/disobey/mango/rust/mango_core.kt` — should return zero matches before regeneration
  </read_first>
  <action>
1. From repo root, run the two `just` recipes:

```bash
just bindings-kotlin
just bindings-swift
```

2. Confirm the four target files changed (git diff):

```bash
git diff --stat ios/Bindings/ android/app/src/main/java/dev/disobey/mango/rust/
```

Expected: only `mango_core.swift`, `mango_coreFFI.h`, `mango_coreFFI.modulemap`, `mango_core.kt` show diffs.

3. Verify the new symbols are present:

```bash
grep -n 'attachImage\|isImage' ios/Bindings/mango_core.swift
grep -n 'AttachImage\|isImage' android/app/src/main/java/dev/disobey/mango/rust/mango_core.kt
```

Both must match at least once each. Swift convention: `case attachImage(filename: String, filePath: String, mimeType: String)` and `public var isImage: Bool`. Kotlin convention: `data class AttachImage(val filename: String, val filePath: String, val mimeType: String) : AppAction()` and `var isImage: Boolean`.

4. If the generators produce incidental unrelated diffs (UniFFI version bumps, comment timestamps), keep them — they're side effects of the regen. Commit the whole diff set together.

5. Sanity build:
   - Kotlin: `cd android && ./gradlew :app:compileDebugKotlin` must succeed. (Android UI code in 31-03 is a separate plan; this only checks binding parse-ability.)
   - Swift: there's no direct CI command to compile Bindings/ standalone; rely on the grep assertion and leave Xcode build to 31-04.
</action>
  <verify>
    <automated>grep -q 'attachImage' /home/lio/g/confidential-app/ios/Bindings/mango_core.swift && grep -q 'isImage' /home/lio/g/confidential-app/ios/Bindings/mango_core.swift && grep -q 'AttachImage' /home/lio/g/confidential-app/android/app/src/main/java/dev/disobey/mango/rust/mango_core.kt && grep -q 'isImage' /home/lio/g/confidential-app/android/app/src/main/java/dev/disobey/mango/rust/mango_core.kt && cd /home/lio/g/confidential-app/android && ./gradlew :app:compileDebugKotlin --quiet</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'attachImage' ios/Bindings/mango_core.swift` >= 1
    - `grep -c 'isImage' ios/Bindings/mango_core.swift` >= 1
    - `grep -c 'AttachImage' android/app/src/main/java/dev/disobey/mango/rust/mango_core.kt` >= 1
    - `grep -c 'isImage' android/app/src/main/java/dev/disobey/mango/rust/mango_core.kt` >= 1
    - `./gradlew :app:compileDebugKotlin` exits 0 (bindings parse cleanly)
  </acceptance_criteria>
  <done>Regenerated binding files are committed; grep proves new symbols exist; Kotlin compiles.</done>
</task>

</tasks>

<verification>
- iOS Bindings/ files updated and committed
- Android mango_core.kt updated and committed
- Android :app:compileDebugKotlin succeeds
- No stale TODO markers or placeholder strings in the regenerated files
</verification>

<success_criteria>
Platform plans (31-03, 31-04) have a symbolically valid AttachImage dispatcher available from Kotlin and Swift without any further Rust work.
</success_criteria>

<output>
After completion, create `.planning/phases/31-multimodal-image-attachments-across-all-platforms-extend-rus/31-02-SUMMARY.md`.
</output>
