---
objective: Add test coverage for streaming cancellation, agent failure injection, and attestation cache TTL behavior

---

# Plan 19-01 — Test Coverage Gaps Implementation

## Goal

Add unit tests for three specific coverage gaps: streaming cancellation in encrypted transports, agent mid-step failures, and attestation cache TTL behavior.

## Tasks

### Task 1: Streaming cancellation tests (TEST-01)

Add tests to `rust/src/tests/streaming.rs`:
- `test_stop_generation_cancels_active_stream`: Verify Tinfoil HPKE transport cancellation path
- `test_stop_generation_cancels_ppq_stream`: Verify PPQ AES-GCM transport cancellation path
- Test StopGeneration → StreamCancelled → BusyState::Idle transition
- Verify streaming_text is preserved after cancel

### Task 2: Agent failure injection tests (TEST-02)

Add tests to `rust/src/tests/agent.rs`:
- `test_agent_max_step_enforcement`: Verify 20-step limit at persistence layer
- Seed 20 agent steps in-memory and verify count >= 20 condition
- Simulate session failure on step limit reached
- Test malformed tool result handling

### Task 3: Attestation cache TTL tests (TEST-03)

Add tests to `rust/src/tests/attestation_cache.rs`:
- `test_get_latest_for_backend_expiry`: Insert expired entry, call get_latest_for_backend, assert returns None
- `test_get_raw_report_bypasses_ttl`: Insert expired entry, call get_raw_report, assert returns blob regardless of TTL

## Success Criteria

1. Streaming cancellation tests verify StopGeneration + StreamCancelled → Idle transition
2. Agent max-step test verifies 20-step limit enforcement
3. Attestation cache TTL tests verify expiry rejection and bypass behavior
4. All tests pass in `cargo test -p mango_core`

## Dependencies

- Existing streaming infrastructure
- Existing agent persistence layer
- Existing attestation cache implementation
