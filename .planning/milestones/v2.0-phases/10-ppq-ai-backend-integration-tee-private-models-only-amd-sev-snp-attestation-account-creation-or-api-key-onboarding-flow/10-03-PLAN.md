---
objective: Create database migrations to seed PPQ.AI backend with correct configuration

---

# Plan 10-03 — Database Migrations for PPQ.AI

## Goal
Create SQLite migrations to seed PPQ.AI backend with initial configuration and update to private transport base URL.

## Tasks

### Task 1: Create MIGRATION_V10
Add MIGRATION_V10 to `rust/src/persistence/schema.rs`:
- Insert PPQ.AI backend row
- id: "ppq-ai"
- name: "PPQ.AI"
- base_url: "https://api.ppq.ai/v1/" (initial public URL)
- tee_type: "AmdSevSnp"
- model_list: NULL (models fetched post-auth)
- display_order: 10
- is_active: 0 (inactive until user enables)

### Task 2: Create MIGRATION_V11
Add MIGRATION_V11 to `rust/src/persistence/schema.rs`:
- UPDATE backends table
- Set base_url to "https://api.ppq.ai/private/v1/" for PPQ.AI
- WHERE id = "ppq-ai" AND base_url = "https://api.ppq.ai/v1/"
- Preserves any user-customized base_url

### Task 3: Register migrations
Add both migrations to the migrations array in `rust/src/persistence/schema.rs`:
- Append MIGRATION_V10 after existing migrations
- Append MIGRATION_V11 after MIGRATION_V10

## Success Criteria
1. MIGRATION_V10 inserts PPQ.AI backend with correct initial configuration
2. MIGRATION_V11 updates PPQ.AI to private transport base URL
3. MIGRATION_V11 preserves user-customized base_url if changed
4. Both migrations are registered in migrations array
5. Migration runner applies both migrations in order

## Dependencies
- Plan 10-02 (provider preset and TEE type)
