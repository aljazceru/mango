---
phase: 28-local-data-encryption-authentication
plan: 04
type: execute
wave: 3
depends_on: ["28-02"]
files_modified:
  - rust/uniffi-bindgen/src/main.rs
  - ios/ConfidentialApp/ContentView.swift
  - ios/ConfidentialApp/LockScreen.swift
  - ios/ConfidentialApp/PinSetupScreen.swift
  - ios/ConfidentialApp/BiometricProviderImpl.swift
  - ios/Bindings/mango_coreFFI.h
  - ios/Bindings/mango_core.swift
autonomous: false
requirements:
  - ENC-07
  - ENC-09
  - ENC-11
  - ENC-12
  - ENC-13
  - ENC-14

must_haves:
  truths:
    - "iOS app shows lock screen on cold launch requiring biometric or PIN to unlock"
    - "First-time users see PIN setup screen with optional duress PIN and biometric enrollment"
    - "BiometricProviderImpl bridges LAContext to the Rust BiometricProvider trait"
    - "Successful unlock restores the previous navigation state"
  artifacts:
    - path: "ios/ConfidentialApp/LockScreen.swift"
      provides: "Lock screen with biometric prompt and PIN input"
    - path: "ios/ConfidentialApp/PinSetupScreen.swift"
      provides: "First-time PIN/password setup with optional duress PIN"
    - path: "ios/ConfidentialApp/BiometricProviderImpl.swift"
      provides: "LAContext-based BiometricProvider implementation"
  key_links:
    - from: "ios/ConfidentialApp/ContentView.swift"
      to: "Screen.locked"
      via: "Switch on screen enum routes to LockScreen"
      pattern: "case .locked"
---

<objective>
Implement the iOS lock screen, PIN setup screen, and BiometricProvider. Regenerate UniFFI
bindings to expose Screen::Locked, PinSetup, BiometricProvider, and all auth AppActions.

Purpose: iOS users see a lock gate on launch, can set up PIN with optional duress PIN,
and unlock via Face ID/Touch ID or PIN entry.

Output: Working iOS lock/unlock flow with biometric and PIN authentication.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/28-local-data-encryption-authentication/28-CONTEXT.md
@.planning/phases/28-local-data-encryption-authentication/28-02-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Regenerate UniFFI bindings and create iOS BiometricProviderImpl</name>
  <files>ios/Bindings/mango_coreFFI.h, ios/Bindings/mango_core.swift, ios/ConfidentialApp/BiometricProviderImpl.swift</files>
  <read_first>
    - rust/src/lib.rs (BiometricProvider trait, Screen enum, AppAction auth variants)
    - ios/ConfidentialApp/ContentView.swift (current screen routing switch)
    - ios/ConfidentialApp/MangoApp.swift or equivalent (FfiApp initialization)
  </read_first>
  <action>
    1. Regenerate UniFFI bindings: `just bindings-swift` (or the project's binding generation command)
    2. Copy updated bindings to ios/Bindings/ directory
    3. Create `ios/ConfidentialApp/BiometricProviderImpl.swift` (per D-11, D-21):
       - Import LocalAuthentication framework
       - Implement `BiometricProvider` protocol from generated bindings
       - `biometricStatus()`: use `LAContext().canEvaluatePolicy(.deviceOwnerAuthenticationWithBiometrics)` -- return "available", "not_enrolled", or "not_available" based on error code
       - `authenticate(reason:)`: use `LAContext().evaluatePolicy(.deviceOwnerAuthenticationWithBiometrics, localizedReason:)` with a DispatchSemaphore to block until callback fires, return Bool
    4. Update FfiApp initialization to pass BiometricProviderImpl instance as the biometric_provider parameter
  </action>
  <verify>
    <automated>cd /home/lio/g/confidential-app && just bindings-swift 2>&1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - File ios/ConfidentialApp/BiometricProviderImpl.swift exists
    - grep -q "LAContext" ios/ConfidentialApp/BiometricProviderImpl.swift
    - grep -q "canEvaluatePolicy" ios/ConfidentialApp/BiometricProviderImpl.swift
    - grep -q "BiometricProvider" ios/Bindings/mango_core.swift (binding generated)
    - grep -q "locked" ios/Bindings/mango_core.swift (Screen.locked variant)
  </acceptance_criteria>
  <done>UniFFI bindings regenerated with all Phase 28 types. iOS BiometricProviderImpl bridges LAContext to Rust.</done>
</task>

<task type="auto">
  <name>Task 2: Create iOS LockScreen and PinSetupScreen with routing</name>
  <files>ios/ConfidentialApp/LockScreen.swift, ios/ConfidentialApp/PinSetupScreen.swift, ios/ConfidentialApp/ContentView.swift</files>
  <read_first>
    - ios/ConfidentialApp/ContentView.swift (screen routing switch statement)
    - ios/ConfidentialApp/SettingsScreen.swift (reference for SwiftUI patterns used in this project)
  </read_first>
  <action>
    **LockScreen.swift (per D-09, D-11, D-24):**
    - SwiftUI View with app icon/name at top
    - On appear: if biometric_available, auto-dispatch `AppAction.attemptBiometricUnlock`
    - PIN/password input: SecureField with "Enter PIN" placeholder
    - Submit button dispatches `AppAction.unlockWithPin(pin: enteredPin)`
    - Show error toast on failed unlock (wired through AppState.toast)
    - No "forgot PIN" link (per security model -- recovery = reinstall)

    **PinSetupScreen.swift (per D-14, D-18):**
    - Step 1: "Set a PIN or password" -- SecureField + confirm field, minimum 4 chars
    - Step 2: "Optional: Set an emergency PIN" -- explanation text: "This PIN will erase all data when entered." SecureField or "Skip" button
    - Validation: duress PIN must differ from real PIN by at least 1 character (per D-18)
    - Step 3: If biometric_available, show "Enable Face ID/Touch ID?" toggle (per D-14)
    - Submit dispatches `AppAction.setupPin(pin:, duressPin:, enableBiometric:)`

    **ContentView.swift routing:**
    Add cases to the screen switch:
    ```swift
    case .locked:
        LockScreen(appState: appState, onDispatchAction: dispatch)
    case .pinSetup:
        PinSetupScreen(appState: appState, onDispatchAction: dispatch)
    ```
  </action>
  <verify>
    <automated>cd /home/lio/g/confidential-app && xcodebuild -project ios/ConfidentialApp.xcodeproj -scheme ConfidentialApp -destination 'platform=iOS Simulator,name=iPhone 16' build 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - File ios/ConfidentialApp/LockScreen.swift exists
    - File ios/ConfidentialApp/PinSetupScreen.swift exists
    - grep -q "attemptBiometricUnlock" ios/ConfidentialApp/LockScreen.swift
    - grep -q "unlockWithPin" ios/ConfidentialApp/LockScreen.swift
    - grep -q "setupPin" ios/ConfidentialApp/PinSetupScreen.swift
    - grep -q "case .locked" ios/ConfidentialApp/ContentView.swift
    - grep -q "case .pinSetup" ios/ConfidentialApp/ContentView.swift
    - iOS build succeeds
  </acceptance_criteria>
  <done>iOS lock screen and PIN setup screen fully functional. Lock screen auto-prompts biometrics, falls back to PIN. PIN setup enforces mandatory PIN with optional duress PIN and biometric enrollment.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>iOS lock screen gate with biometric/PIN unlock and first-time PIN setup flow</what-built>
  <how-to-verify>
    1. Run iOS app in simulator (fresh install -- delete app data first)
    2. Verify PinSetupScreen appears after onboarding
    3. Set a PIN, optionally set duress PIN, enable biometrics
    4. Kill and relaunch app -- verify LockScreen appears
    5. Enter correct PIN -- verify app unlocks to home screen
    6. Background app for >5 min (or set timeout to "Immediately" in settings) -- verify lock screen returns
    7. If duress PIN was set: enter duress PIN -- verify all data wiped and app resets to onboarding
  </how-to-verify>
  <resume-signal>Type "approved" or describe issues</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| iOS LAContext -> Rust | Biometric auth result crosses platform boundary |
| User PIN input -> Rust actor | PIN string sent via AppAction |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-28-17 | Spoofing | BiometricProviderImpl | accept | LAContext is Apple's trusted API; no additional verification possible from Rust |
| T-28-18 | Information Disclosure | LockScreen PIN field | mitigate | Use SecureField (masked input); PIN never stored in SwiftUI @State beyond the submit action |
| T-28-19 | Denial of Service | PinSetupScreen | accept | No rate limiting on setup; user sets PIN once |
</threat_model>

<verification>
- iOS app builds and launches to lock screen
- PIN setup flow completes successfully
- Biometric prompt appears on supported devices
- Unlock restores previous navigation state
</verification>

<success_criteria>
- Fresh install shows PinSetupScreen after onboarding
- Returning user sees LockScreen on cold launch
- Biometric unlock works on Face ID/Touch ID devices
- PIN unlock works on all devices
- Duress PIN triggers immediate wipe
</success_criteria>

<output>
After completion, create `.planning/phases/28-local-data-encryption-authentication/28-04-SUMMARY.md`
</output>
