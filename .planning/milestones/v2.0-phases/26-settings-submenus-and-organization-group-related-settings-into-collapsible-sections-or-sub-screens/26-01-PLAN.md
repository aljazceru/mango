---
phase: 26-settings-submenus-and-organization
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - rust/src/lib.rs
  - ios/Bindings/mango_core.swift
  - ios/Bindings/mango_coreFFI.h
  - ios/Bindings/mango_coreFFI.modulemap
  - android/app/src/main/java/dev/disobey/mango/rust/mango_core.kt
autonomous: true
requirements: []

must_haves:
  truths:
    - "Screen enum has SettingsProviders and SettingsDefaults variants"
    - "UniFFI bindings compile with new Screen variants on all platforms"
  artifacts:
    - path: "rust/src/lib.rs"
      provides: "Screen::SettingsProviders and Screen::SettingsDefaults enum variants"
      contains: "SettingsProviders"
    - path: "ios/Bindings/mango_core.swift"
      provides: "Swift bindings with settingsProviders and settingsDefaults Screen cases"
      contains: "settingsProviders"
    - path: "android/app/src/main/java/dev/disobey/mango/rust/mango_core.kt"
      provides: "Kotlin bindings with SettingsProviders and SettingsDefaults Screen variants"
      contains: "SettingsProviders"
  key_links:
    - from: "rust/src/lib.rs"
      to: "ios/Bindings/mango_core.swift"
      via: "UniFFI bindings generation"
      pattern: "SettingsProviders"
    - from: "rust/src/lib.rs"
      to: "android/app/src/main/java/dev/disobey/mango/rust/mango_core.kt"
      via: "UniFFI bindings generation"
      pattern: "SettingsProviders"
---

<objective>
Add SettingsProviders and SettingsDefaults variants to the Rust Screen enum and regenerate UniFFI bindings for iOS and Android.

Purpose: Wave 2 plans need these Screen variants to exist in the generated bindings before implementing sub-screen navigation on all three platforms.
Output: Updated Screen enum in Rust, regenerated Swift and Kotlin bindings.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/26-settings-submenus-and-organization-group-related-settings-into-collapsible-sections-or-sub-screens/26-RESEARCH.md

<interfaces>
<!-- Current Screen enum from rust/src/lib.rs (line 360) -->
```rust
#[derive(uniffi::Enum, Clone, Debug, PartialEq)]
pub enum Screen {
    Home,
    Settings,
    Chat { conversation_id: String },
    Onboarding { step: OnboardingStep },
    Documents,
    Agents,
    Memories,
}
```

<!-- UniFFI bindings generation commands from justfile -->
```
just bindings-swift   # builds host release, generates ios/Bindings/
just bindings-kotlin  # builds host release, generates android/app/src/main/java/
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add SettingsProviders and SettingsDefaults Screen variants and regenerate bindings</name>
  <files>rust/src/lib.rs, ios/Bindings/mango_core.swift, ios/Bindings/mango_coreFFI.h, ios/Bindings/mango_coreFFI.modulemap, android/app/src/main/java/dev/disobey/mango/rust/mango_core.kt</files>
  <read_first>rust/src/lib.rs, justfile</read_first>
  <action>
1. Open `rust/src/lib.rs` and find the `Screen` enum (line ~360). Add two new variants after `Memories`:

```rust
/// Provider management sub-screen — pushed from Settings main screen (Phase 26).
SettingsProviders,
/// Defaults sub-screen (model picker + default instructions) — pushed from Settings (Phase 26).
SettingsDefaults,
```

The enum should now have 9 variants: Home, Settings, Chat, Onboarding, Documents, Agents, Memories, SettingsProviders, SettingsDefaults.

2. Verify Rust compiles: `cargo check -p mango_core`

3. Regenerate UniFFI bindings:
   - `just bindings-swift` — produces updated `ios/Bindings/mango_core.swift`, `ios/Bindings/mango_coreFFI.h`, `ios/Bindings/mango_coreFFI.modulemap`
   - `just bindings-kotlin` — produces updated `android/app/src/main/java/dev/disobey/mango/rust/mango_core.kt`

4. Verify bindings contain the new variants:
   - `grep -c "settingsProviders\|settingsDefaults" ios/Bindings/mango_core.swift` should return > 0
   - `grep -c "SettingsProviders\|SettingsDefaults" android/app/src/main/java/dev/disobey/mango/rust/mango_core.kt` should return > 0
  </action>
  <verify>
    <automated>cargo check -p mango_core && grep "settingsProviders" ios/Bindings/mango_core.swift && grep "SettingsProviders" android/app/src/main/java/dev/disobey/mango/rust/mango_core.kt</automated>
  </verify>
  <acceptance_criteria>
    - rust/src/lib.rs contains `SettingsProviders,` as a Screen enum variant
    - rust/src/lib.rs contains `SettingsDefaults,` as a Screen enum variant
    - `cargo check -p mango_core` exits 0
    - ios/Bindings/mango_core.swift contains `settingsProviders` (Swift enum case)
    - ios/Bindings/mango_core.swift contains `settingsDefaults` (Swift enum case)
    - android/app/src/main/java/dev/disobey/mango/rust/mango_core.kt contains `SETTINGS_PROVIDERS` or `SettingsProviders`
    - android/app/src/main/java/dev/disobey/mango/rust/mango_core.kt contains `SETTINGS_DEFAULTS` or `SettingsDefaults`
  </acceptance_criteria>
  <done>Screen enum has 9 variants, Rust compiles, Swift and Kotlin bindings regenerated with new screen cases</done>
</task>

</tasks>

<verification>
- `cargo check -p mango_core` passes
- `cargo test -p mango_core 2>&1 | tail -5` shows all tests pass (no regressions)
- Swift bindings contain settingsProviders and settingsDefaults
- Kotlin bindings contain SettingsProviders and SettingsDefaults
</verification>

<success_criteria>
- Screen enum extended with SettingsProviders and SettingsDefaults
- UniFFI bindings regenerated for both platforms
- All existing tests still pass
</success_criteria>

<output>
After completion, create `.planning/phases/26-settings-submenus-and-organization-group-related-settings-into-collapsible-sections-or-sub-screens/26-01-SUMMARY.md`
</output>
