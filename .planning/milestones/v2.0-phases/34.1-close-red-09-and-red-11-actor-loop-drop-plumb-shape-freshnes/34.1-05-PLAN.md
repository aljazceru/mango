---
phase: 34.1
plan: 05
type: execute
wave: 4
depends_on: ["34.1-02", "34.1-03"]
files_modified:
  - ios/Mango/Mango/SettingsProvidersView.swift
  - ios/Mango/Mango/AttestationBadgeView.swift
  - ios/Mango/Mango/AppColors.swift
  - ios/Mango/Mango/ChatView.swift
  - ios/Mango/Mango/ModelPickerView.swift
  - ios/Mango/Mango/OnboardingView.swift
autonomous: true
requirements: ["RED-09", "RED-11"]
requirements_addressed: ["RED-09", "RED-11"]
must_haves:
  truths:
    - "iOS Settings → Providers screen renders 'Verified for this enclave instance' sub-line when status is Verified with freshness == 'PerEnclave'"
    - "iOS Settings → Providers screen renders 'gateway ✓ • model ✓ • compose ✓' sub-line when status is Verified with non-empty orchestrated_components"
    - "All other Swift case .verified pattern-match sites compile against the new associated-values binding"
  artifacts:
    - path: "ios/Mango/Mango/SettingsProvidersView.swift"
      provides: "Two new conditional Text views under the row attestation label"
      contains: "Verified for this enclave instance"
  key_links:
    - from: "AttestationStatus.verified (Swift binding)"
      to: "Settings providers row body"
      via: "if case let .verified(...) = status"
      pattern: "case .verified"
---

<objective>
Mirror the Android sub-line rendering on iOS SwiftUI per UI-SPEC.md (LOCKED copy). Update every Swift `case .verified` pattern site to bind associated values (compile-fix sweep — no behavior change in chat / onboarding / model picker / badge).

Purpose: Closes RED-09 and RED-11 on iOS.
Output: Two new conditional Text views in SettingsProvidersView.swift; compile-fix updates in five other Swift files.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/34.1-close-red-09-and-red-11-actor-loop-drop-plumb-shape-freshnes/34.1-CONTEXT.md
@.planning/phases/34.1-close-red-09-and-red-11-actor-loop-drop-plumb-shape-freshnes/34.1-RESEARCH.md
@.planning/phases/34.1-close-red-09-and-red-11-actor-loop-drop-plumb-shape-freshnes/34.1-PATTERNS.md
@.planning/phases/34.1-close-red-09-and-red-11-actor-loop-drop-plumb-shape-freshnes/34.1-UI-SPEC.md
@.planning/phases/34.1-close-red-09-and-red-11-actor-loop-drop-plumb-shape-freshnes/34.1-02-SUMMARY.md

@ios/Mango/Mango/SettingsProvidersView.swift
@ios/Mango/Mango/AttestationBadgeView.swift
@ios/Mango/Mango/AppColors.swift
@ios/Mango/Mango/ChatView.swift
@ios/Mango/Mango/ModelPickerView.swift
@ios/Mango/Mango/OnboardingView.swift

<interfaces>
LOCKED copy (UI-SPEC.md):
- Freshness sub-line: "Verified for this enclave instance" (only when freshness == "PerEnclave")
- Orchestrated breakdown: "gateway ✓ • model ✓ • compose ✓"
- Component label map: compose_manager → compose; unknown labels verbatim
- Separator: " • " (space U+2022 space); glyph: " ✓" (space U+2713)

Typography:
- .font(.caption2)
- .foregroundColor(.secondary)

Insertion point: after the existing attestation-style label rendering in the providers row body (around line 241 of SettingsProvidersView.swift). Sub-lines stack vertically.

Swift binding pattern (post-bindgen — verify shape from regenerated 34.1-02 bindings under ios/Bindings/):
After 34.1-02, AttestationStatus.verified gains three associated values: shape: String?, freshness: String?, orchestratedComponents: [OrchestratedComponent]?. Use `if case let .verified(shape, freshness, components) = status` to destructure.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Compile-fix sweep for case .verified in non-providers Swift files</name>
  <files>ios/Mango/Mango/AttestationBadgeView.swift, ios/Mango/Mango/AppColors.swift, ios/Mango/Mango/ChatView.swift, ios/Mango/Mango/ModelPickerView.swift, ios/Mango/Mango/OnboardingView.swift</files>
  <read_first>
    - ios/Mango/Mango/AttestationBadgeView.swift (lines 6, 79 — case .verified)
    - ios/Mango/Mango/AppColors.swift (lines 83, 97, 111 — switch on AttestationStatus, color helpers)
    - ios/Mango/Mango/ChatView.swift (lines 144, 292)
    - ios/Mango/Mango/ModelPickerView.swift (line 11)
    - ios/Mango/Mango/OnboardingView.swift (line 292)
    - The regenerated Swift bindings under ios/Bindings/
  </read_first>
  <action>
    For each file, every bare `case .verified:` (without associated-value binding) gets updated to `case .verified(_, _, _):` since the case body does not consume the new fields. None of these five files render sub-lines (out of scope per UI-SPEC visibility rules).

    Specifically:
    - AppColors.swift:83, 97, 111: switch on AttestationStatus returns a color. Change `case .verified:` to `case .verified(_, _, _):`. No behavior change.
    - AttestationBadgeView.swift:6, 79: badge stays single-line per CONTEXT.md. Same compile-fix only.
    - ChatView.swift:144, 292: status used for color/icon only.
    - ModelPickerView.swift:11: optional status; check if pattern-match exists and update.
    - OnboardingView.swift:292: same pattern, no sub-fields displayed during onboarding.

    DO NOT add any sub-line rendering in these five files.

    Build verify: `cd ios && xcodebuild -scheme Mango -sdk iphonesimulator build 2>&1 | tail -50` if Xcode CLI is available; otherwise rely on grep-based verify and final manual Xcode build.
  </action>
  <verify>
    <automated>! grep -nE "case [.]verified:" ios/Mango/Mango/AttestationBadgeView.swift ios/Mango/Mango/AppColors.swift ios/Mango/Mango/ChatView.swift ios/Mango/Mango/ModelPickerView.swift ios/Mango/Mango/OnboardingView.swift</automated>
  </verify>
  <acceptance_criteria>
    - `grep -nE "case [.]verified:" ios/Mango/Mango/{AttestationBadgeView,AppColors,ChatView,ModelPickerView,OnboardingView}.swift` returns ZERO hits (every bare case .verified is now bound).
    - `grep -nE "case [.]verified" ios/Mango/Mango/{AttestationBadgeView,AppColors,ChatView,ModelPickerView,OnboardingView}.swift` returns at least one hit per file that previously had one (presence preserved, just bound).
    - If `xcodebuild` is locally available: build succeeds for these files (errors in SettingsProvidersView.swift acceptable here — Task 2 fixes them).
  </acceptance_criteria>
  <done>Five Swift files compile against the new associated-values binding; behavior unchanged.</done>
</task>

<task type="auto">
  <name>Task 2: Render freshness + orchestrated breakdown sub-lines in SettingsProvidersView.swift</name>
  <files>ios/Mango/Mango/SettingsProvidersView.swift</files>
  <read_first>
    - ios/Mango/Mango/SettingsProvidersView.swift (lines 220-267; especially the row body around line 241 and the `attestationStyle` private fn)
    - .planning/phases/34.1-close-red-09-and-red-11-actor-loop-drop-plumb-shape-freshnes/34.1-UI-SPEC.md
    - .planning/phases/34.1-close-red-09-and-red-11-actor-loop-drop-plumb-shape-freshnes/34.1-PATTERNS.md (section "ios/Mango/Mango/SettingsProvidersView.swift")
    - The regenerated Swift binding for AttestationStatus and OrchestratedComponent under ios/Bindings/
  </read_first>
  <action>
    Inside the providers row body, AFTER the existing `Text(label)` that uses `attestationStyle(...)` (around line 241), insert the sub-line block.

    Use this structure (verify associated-value labeling against the regenerated Swift binding — UniFFI sometimes produces labeled associated values like `case verified(shape: String?, freshness: String?, orchestratedComponents: [OrchestratedComponent]?)`):

    ```swift
    // Phase 34.1: trust-UI sub-lines for Redpill freshness + orchestrated breakdown.
    // Copy LOCKED in 34.1-UI-SPEC.md.
    if case let .verified(_, freshness, components) = att.status {
        if freshness == "PerEnclave" {
            Text("Verified for this enclave instance")
                .font(.caption2)
                .foregroundColor(.secondary)
        }
        if let comps = components, !comps.isEmpty {
            let labelMap: [String: String] = [
                "gateway": "gateway",
                "model": "model",
                "compose_manager": "compose",
            ]
            let line = comps
                .map { "\(labelMap[$0.label] ?? $0.label) ✓" }
                .joined(separator: " • ")
            Text(line)
                .font(.caption2)
                .foregroundColor(.secondary)
        }
    }
    ```

    If the regenerated UniFFI Swift binding uses LABELED associated values, the destructure may need to be:
    `if case let .verified(shape: _, freshness: freshness, orchestratedComponents: components) = att.status { ... }`
    Read the regenerated ios/Bindings/ AttestationStatus declaration to determine which form compiles.

    For `attestationStyle(_ s: AttestationStatus, _ scheme: ColorScheme)` private fn at lines 241-248: change `case .verified:` to `case .verified(_, _, _):` (compile-fix; no behavior change). The label/color returned is unchanged.

    Substitute property names if UniFFI's Swift output uses something other than `label`/`value` for OrchestratedComponent fields — read the regenerated binding to confirm.

    Verify code path: the parent context must be a SwiftUI View body. If the row is built inside a non-View context, wrap the new code in a `VStack(alignment: .leading, spacing: 4) { ... }` and embed it in the row body. Read the surrounding 30 lines before deciding placement.

    DO NOT modify AttestationBadgeView (badge stays single-line per CONTEXT.md).
  </action>
  <verify>
    <automated>grep -F "Verified for this enclave instance" ios/Mango/Mango/SettingsProvidersView.swift && grep -F "compose_manager" ios/Mango/Mango/SettingsProvidersView.swift && grep -F "PerEnclave" ios/Mango/Mango/SettingsProvidersView.swift && grep -F " ✓" ios/Mango/Mango/SettingsProvidersView.swift && grep -F " • " ios/Mango/Mango/SettingsProvidersView.swift</automated>
  </verify>
  <acceptance_criteria>
    - `grep -F "Verified for this enclave instance" ios/Mango/Mango/SettingsProvidersView.swift` returns exactly one hit (locked copy).
    - `grep -F " ✓" ios/Mango/Mango/SettingsProvidersView.swift` returns at least one hit (per-component success glyph).
    - `grep -F " • " ios/Mango/Mango/SettingsProvidersView.swift` returns at least one hit (separator).
    - `grep -F "compose_manager" ios/Mango/Mango/SettingsProvidersView.swift` returns at least one hit (label map source key).
    - `grep -F "PerEnclave" ios/Mango/Mango/SettingsProvidersView.swift` returns at least one hit (render condition).
    - `grep -E "case [.]verified:" ios/Mango/Mango/SettingsProvidersView.swift` returns ZERO hits (all matches now bind associated values).
    - If `xcodebuild` is available locally, full iOS build succeeds. Otherwise manual Xcode build is required at phase verification time.
  </acceptance_criteria>
  <done>Two sub-lines render under the row attestation label with locked copy; SettingsProvidersView.swift compiles; badge unchanged.</done>
</task>

</tasks>

<verification>
- All six grep checks above pass on SettingsProvidersView.swift.
- No bare `case .verified:` remains in any of the six modified Swift files.
- Manual Xcode build smoke (deferred to phase verification): build the app, navigate to Settings → Providers, observe the two sub-lines render under the locked conditions.
</verification>

<success_criteria>
1. SettingsProvidersView.swift renders both sub-lines under locked render conditions with locked copy.
2. AttestationBadgeView / AppColors / ChatView / ModelPickerView / OnboardingView compile (compile-fix only).
3. compose_manager → compose mapping applied; unknown labels fallback to verbatim.
4. Badge widget remains single-line.
</success_criteria>

<output>
After completion, create `.planning/phases/34.1-close-red-09-and-red-11-actor-loop-drop-plumb-shape-freshnes/34.1-05-SUMMARY.md`
</output>
