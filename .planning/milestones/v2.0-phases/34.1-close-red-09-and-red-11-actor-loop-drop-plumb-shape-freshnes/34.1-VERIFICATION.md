---
phase: 34.1-close-red-09-and-red-11-actor-loop-drop-plumb-shape-freshnes
verified: 2026-04-27T00:00:00Z
status: human_needed
score: 9/9 automated must-haves verified (5 manual UI smoke gates pending per VALIDATION.md)
overrides_applied: 0
human_verification:
  - test: "Android trust UI freshness sub-line renders on PerEnclave"
    expected: "On a Chutes-routed Redpill model, Settings → Providers expanded row shows 'Verified for this enclave instance' sub-line under the Verified pill."
    why_human: "No Compose snapshot/UI test infra in repo; visual confirmation only."
  - test: "Android trust UI orchestrated three-quote breakdown"
    expected: "On an Orchestrated-shape Redpill model, sub-line reads 'gateway ✓ • model ✓ • compose ✓'."
    why_human: "Visual rendering; no snapshot infra."
  - test: "iOS sub-line + breakdown"
    expected: "Same renderings on iOS SwiftUI providers screen."
    why_human: "No XCTest snapshot infra; visual confirmation only."
  - test: "Desktop iced sub-line + breakdown"
    expected: "Same renderings in desktop iced providers view."
    why_human: "No iced UI test infra; visual confirmation only."
  - test: "Live Shape A/B/C + Tinfoil refusal (Phase 34 SC #10 — carried forward)"
    expected: "`cargo test -p mango_core --lib live_redpill -- --ignored --nocapture` runs all 4 ignored tests against api.redpill.ai."
    why_human: "Live network + paid API key; cannot run inside sandbox."
---

# Phase 34.1 — Verification Report

**Phase Goal:** Close RED-09 and RED-11 actor-loop drop. Plumb `shape`/`freshness`/`orchestrated_components` from `AttestationEvent::Verified` through actor loop → UniFFI `AttestationStatus` → `AttestationRecord` (SQLite) → native trust UI on Android, iOS, and desktop iced. Cryptographic verification path was LOCKED (untouched).

**Verified:** 2026-04-27
**Status:** human_needed (all automated must-haves green; only manual UI smoke gates outstanding per VALIDATION.md)
**Re-verification:** No — initial verification.

---

## Goal Achievement

### Observable Truths

| #   | Truth | Status | Evidence |
| --- | ----- | ------ | -------- |
| 1   | Actor loop no longer drops `shape`/`freshness`/`orchestrated_components` (no `_` binds; no "deferred" comment) | ✓ VERIFIED | `grep -n "deferred to future cache columns\|shape: _\|freshness: _\|orchestrated_components: _" rust/src/lib.rs` → no matches. `rust/src/lib.rs:7401-7414` now calls `attestation::map_event_to_record_and_status(att_event, now_secs)` and threads the values into AppState + cache. |
| 2   | `AttestationStatus::Verified` is a struct variant with three optional fields, exposed across UniFFI to Kotlin and Swift | ✓ VERIFIED | `rust/src/attestation/mod.rs:39-52` — `#[derive(uniffi::Enum)]` enum with `Verified { shape: Option<String>, freshness: Option<String>, orchestrated_components: Option<Vec<OrchestratedComponent>> }`. `OrchestratedComponent` is a `uniffi::Record` with `label`/`value`. Kotlin call sites (`SettingsProvidersScreen.kt:200,210`) and Swift (`SettingsProvidersView.swift:162-176`) destructure the variant. |
| 3   | `AttestationRecord` (Rust struct + SQLite `attestation_cache`) carries the three fields; pre-V19 rows hydrate to None | ✓ VERIFIED | `rust/src/attestation/mod.rs:65-84` — `AttestationRecord` has `shape`, `freshness`, `orchestrated_components` Option fields. `rust/src/persistence/schema.rs:347-350` — `MIGRATION_V19` adds three nullable TEXT cols; registered in migrations vec at line 373. `rust/src/attestation/cache.rs:285-294` — round-trip test with explicit pre-V19 fallback to `None`. |
| 4   | Helper `map_event_to_record_and_status` produces a self-consistent `AttestationRecord` (record.status matches record.shape/freshness/orchestrated_components) | ✓ VERIFIED | `rust/src/attestation/mod.rs:149-218` — helper builds `status_uniffi` once and assigns it to both `record.status` and the outer return value, while cloning the underlying `shape`/`freshness`/`orchestrated_components` into the record's own typed fields (`Vec<(String, String)>`) and the UniFFI-friendly `Vec<OrchestratedComponent>`. Single source — cannot drift. |
| 5a  | Android Compose renders "Verified for this enclave instance" on PerEnclave AND "gateway ✓ • model ✓ • compose ✓" on Orchestrated | ✓ VERIFIED (code path) | `android/app/src/main/java/dev/disobey/mango/ui/SettingsProvidersScreen.kt:197-227` — pattern-matches `AttestationStatus.Verified`, conditionally renders sub-line on `freshness == "PerEnclave"`, joins components with `" • "` and `✓` glyph using `labelMap`. |
| 5b  | iOS SwiftUI renders the same | ✓ VERIFIED (code path) | `ios/Mango/Mango/SettingsProvidersView.swift:159-182` — `if case let .verified(_, freshness, components) = att.status` destructure; identical labelMap and copy. |
| 5c  | Desktop iced renders the same | ✓ VERIFIED (code path) | `desktop/iced/src/views/settings_providers.rs:341-363` — `if let Some(AttestationStatus::Verified { freshness, orchestrated_components, .. })` match; conditional `right_col.push(...)` for both sub-lines. `orchestrated_breakdown_line` helper at line 54. |
| 6   | Phase 34's RED-09 and RED-11 must-haves are now FULL | ✓ VERIFIED | Phase 34 SUMMARY (`34-SUMMARY.md:25-35`) explicitly states "RED-09 and RED-11 are FULL as of Phase 34.1." Cryptographic path was already complete in Phase 34; the UI surfacing chain that was missing is now end-to-end (truths 1–5). |
| 7   | All Rust tests in mango_core pass | ✓ VERIFIED | `cargo test -p mango_core --lib` → **387 passed; 0 failed; 18 ignored**; runtime 15.11s. (Up from 382 in Phase 34 — five new tests for 34.1: actor-loop threading, Verified-variant access, V19 round-trip + pre-V19 fallback, helper self-consistency.) |
| 8   | Cryptographic verification logic is untouched | ✓ VERIFIED | `git log` since `49c19cc` (Phase 34 close) shows zero commits modifying `rust/src/attestation/redpill.rs`, `rust/src/attestation/venice.rs`, `rust/src/attestation/tdx.rs`, `rust/src/attestation/nvidia.rs`, or the four `verify_*` decoders / `verify_backend_attestation` orchestrator. All 34.1 commits scope to `lib.rs` actor-loop, `attestation/mod.rs` types + helper, `attestation/cache.rs`, `persistence/schema.rs`, native UI files, and bindings. |
| 9   | STATE.md and Phase 34 SUMMARY.md reflect closure | ✓ VERIFIED | `.planning/STATE.md:6,24-30,160-165` — records Phase 34.1 complete, all RED-09/RED-11 FULL. `34-SUMMARY.md:25-35` carries the explicit closure block referencing 34.1-01..34.1-06. |

**Score:** 9/9 automated truths verified.

### Required Artifacts

| Artifact | Expected | Status | Details |
| -------- | -------- | ------ | ------- |
| `rust/src/attestation/mod.rs` | `OrchestratedComponent` (`uniffi::Record`); `AttestationStatus::Verified` struct variant; `AttestationRecord` with 3 new fields; `AttestationEvent::Verified` carrying 3 fields; `map_event_to_record_and_status` helper | ✓ VERIFIED | All present at lines 29-218; helper is single source mapping event → (status, record). |
| `rust/src/lib.rs` (actor handler) | Calls helper; no `_` drops; no "deferred" comment | ✓ VERIFIED | Lines 7401-7414 reduced to single helper call; comment block at 7416-7425 explains stickiness logic only — no surfacing-deferral. `grep "deferred to future cache columns"` → empty. |
| `rust/src/attestation/cache.rs` | Read/write paths bind 3 columns; pre-V19 rows hydrate to `None` | ✓ VERIFIED | Lines 62-108 (`get`), 140-189 (`get_for_backend`), 211-225 (`put`); JSON encode/decode of `Vec<(String, String)>` for `orchestrated_components`. |
| `rust/src/persistence/schema.rs` | `MIGRATION_V19` adds 3 nullable TEXT cols; registered in migrations vec | ✓ VERIFIED | Lines 341-373; tests at 452-463 confirm columns exist after migration. |
| `android/app/src/main/java/dev/disobey/mango/ui/SettingsProvidersScreen.kt` | Pattern-matches struct variant; renders sub-lines per UI-SPEC | ✓ VERIFIED | Lines 197-227. |
| `ios/Mango/Mango/SettingsProvidersView.swift` | Pattern-matches `.verified(_, freshness, components)`; renders sub-lines | ✓ VERIFIED | Lines 159-182. |
| `desktop/iced/src/views/settings_providers.rs` | Destructures struct variant; renders sub-lines via `right_col.push(...)`; `orchestrated_breakdown_line` helper | ✓ VERIFIED | Lines 54-77, 338-363. |
| Generated Kotlin / Swift bindings | Updated to reflect struct variant | ✓ VERIFIED | Commit `1758c06` regenerates bindings; iOS file `ios/Bindings/mango_core.swift` contains `OrchestratedComponent` + new variant; native consumers compile. |

### Key Link Verification

| From | To | Via | Status | Details |
| ---- | -- | --- | ------ | ------- |
| `AttestationEvent::Verified` (with 3 fields) | `AttestationStatus::Verified` (UniFFI) | `map_event_to_record_and_status` (mod.rs:149-206) | ✓ WIRED | Helper destructures all named fields; emits struct variant with cloned Options. |
| `AttestationEvent::Verified` | `AttestationRecord` SQLite write | helper → `cache.put(&record)` at lib.rs:7496 | ✓ WIRED | record.status + record.shape/freshness/orchestrated_components both populated from same source. |
| `AttestationCache` row | `AttestationStatus::Verified` re-hydration | row mapper builds variant directly from columns (cache.rs:80-92) | ✓ WIRED | Pre-V19 rows return `None` subfields (cache.rs:285-294). |
| `AppState.attestationStatuses[i].status` | Android Compose `ProviderStatusPill` host (column) | `verified.freshness == "PerEnclave"`, `verified.orchestratedComponents` | ✓ WIRED | Direct property reads on the struct-variant in Kotlin. |
| `appState.attestationStatuses` | iOS SwiftUI sub-line | `if case let .verified(_, freshness, components)` | ✓ WIRED | Pattern-match. |
| `att_status` | Desktop iced sub-line | `if let Some(AttestationStatus::Verified { freshness, orchestrated_components, .. })` | ✓ WIRED | Direct match. |

### Data-Flow Trace (Level 4)

| Artifact | Data Variable | Source | Produces Real Data | Status |
| -------- | ------------- | ------ | ------------------ | ------ |
| `map_event_to_record_and_status` | `shape`/`freshness`/`orchestrated_components` | `AttestationEvent::Verified` (already populated by `verify_backend_attestation` per Phase 34) | Yes — Option-wrapped strings + `Vec<(label, hex)>` | ✓ FLOWING |
| Android sub-line text | `verified.freshness`, `verified.orchestratedComponents` | `AppState.attestationStatuses[i].status` (refreshed every actor emit) | Yes — non-static; conditional render only when populated | ✓ FLOWING |
| iOS sub-line text | `freshness`, `components` from pattern-match | Same `AppState.attestationStatuses` | Yes | ✓ FLOWING |
| Desktop sub-line text | `freshness`/`orchestrated_components` from `AttestationStatus::Verified { .. }` match | Same | Yes | ✓ FLOWING |
| SQLite re-hydration on app restart | `shape`/`freshness`/`orchestrated_components` columns | V19 row in `attestation_cache` | Yes — bound from typed columns; JSON for components | ✓ FLOWING |

### Behavioral Spot-Checks

| Behavior | Command | Result | Status |
| -------- | ------- | ------ | ------ |
| Full mango_core lib suite passes | `cargo test -p mango_core --lib` | 387 passed; 0 failed; 18 ignored; 15.11s | ✓ PASS |
| No `_`-bind drops or stale "deferred" comment in actor loop | `grep -n "deferred to future cache columns\|shape: _\|freshness: _\|orchestrated_components: _" rust/src/lib.rs` | (empty) | ✓ PASS |
| `MIGRATION_V19` adds the three columns and is registered | `grep -n "MIGRATION_V19\|ALTER TABLE attestation_cache" rust/src/persistence/schema.rs` | 4 matches at lines 347-373 | ✓ PASS |
| All three native UIs hold the locked copy | `grep -rn "Verified for this enclave instance" android/ ios/ desktop/` | 3 matches (one per platform) | ✓ PASS |
| Crypto verifier untouched since Phase 34 close | `git log 49c19cc..HEAD -- rust/src/attestation/redpill.rs rust/src/attestation/venice.rs rust/src/attestation/tdx.rs rust/src/attestation/nvidia.rs` | (empty) | ✓ PASS |

### Requirements Coverage

| Requirement | Description | Status | Evidence |
| ----------- | ----------- | ------ | -------- |
| RED-09 | Chutes models display "freshness valid for enclave lifetime" sub-line in trust UI | ✓ SATISFIED (FULL) | Cryptographic path already verified in Phase 34. UI surface delivered: `freshness == "PerEnclave"` → "Verified for this enclave instance" on Android (line 202-208), iOS (line 163-167), Desktop (line 347-353). UniFFI `AttestationStatus::Verified.freshness` carries the value across the boundary. |
| RED-11 | Orchestrated-shape models show three-quote breakdown (gateway / model / compose_manager) | ✓ SATISFIED (FULL) | `OrchestratedComponent { label, value }` carried across UniFFI. All three UIs render `"<label> ✓ • <label> ✓ • <label> ✓"` with locked label map (`gateway`, `model`, `compose_manager → compose`). Persisted across app restarts via V19 JSON column. |

### Anti-Patterns Found

None. The previous Phase 34 anti-pattern (`_` drops + "deferred to future cache columns" comment at lib.rs:7421-7427) has been fully resolved. Module-wide `#![allow(dead_code)]` on `attestation/redpill.rs` was inherited from Phase 34 and is unchanged in this phase (locked path); not a 34.1 regression.

### Human Verification Required

1. **Android freshness sub-line on PerEnclave**
   - Action: Build debug APK, add Redpill backend, run a Chutes-routed model.
   - Expected: Settings → Providers expanded row shows "Verified for this enclave instance" under the Verified pill.
   - Why human: No Compose snapshot infra; visual confirmation only.

2. **Android orchestrated three-quote breakdown**
   - Action: Same APK, run an Orchestrated-shape Redpill model.
   - Expected: Sub-line "gateway ✓ • model ✓ • compose ✓".
   - Why human: Visual.

3. **iOS sub-line + breakdown**
   - Action: Build iOS sim, repeat scenarios.
   - Expected: Identical sub-lines render.
   - Why human: No XCTest snapshot infra.

4. **Desktop iced sub-line + breakdown**
   - Action: Build desktop, repeat scenarios.
   - Expected: Identical sub-lines render.
   - Why human: No iced UI test infra.

5. **Live Shape A/B/C + Tinfoil refusal (carried from Phase 34 SC #10)**
   - Action: `cargo test -p mango_core --lib live_redpill -- --ignored --nocapture` with `REDPILL_API_KEY` set.
   - Expected: All four ignored tests pass; logs show `[live] shape=… freshness=…`.
   - Why human: Live network + paid API key.

### Gaps Summary

No automated gaps. The Phase 34 RED-09 and RED-11 PARTIAL gaps are now FULL: the actor-loop drop is gone (helper-driven), the UniFFI boundary carries the data via a struct variant, SQLite V19 persists it across restarts, and three native UIs render the locked sub-line copy. The full Rust test suite is green at 387 passed (5 new tests for this phase). The cryptographic verifier remained LOCKED — no commits touched `verify_*`, `verify_backend_attestation`, decoders, or `attestation/redpill.rs` since Phase 34 closed.

The status is `human_needed` purely because VALIDATION.md explicitly registered five manual smoke gates (4 cross-platform UI visual confirmations + the Phase 34 live-network suite carried forward). Per the verification decision tree, any non-empty human verification list forces `human_needed` even when the automated score is N/N.

---

## VERIFICATION PASSED

All nine automated must-haves are VERIFIED. No gaps. Five manual smoke gates remain pending per VALIDATION.md (4 cross-platform UI visuals + Phase 34's deferred live-network suite). Phase status returned as `human_needed` to surface those — the codebase changes themselves are complete and goal-consistent.

*Verified: 2026-04-27 — verifier (goal-backward, not blind-trust)*
