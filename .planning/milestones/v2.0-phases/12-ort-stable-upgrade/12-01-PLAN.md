---
phase: 12-ort-stable-upgrade
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - rust/Cargo.toml
  - Cargo.lock
  - CLAUDE.md
  - .planning/workstreams/milestone/ROADMAP.md
  - .planning/workstreams/milestone/REQUIREMENTS.md
autonomous: true
requirements:
  - DFNS-03
must_haves:
  truths:
    - "ort version declared in rust/Cargo.toml matches the actual resolved version in Cargo.lock (2.0.0-rc.11)"
    - "All 166+ existing tests pass without modification after the Cargo.toml update"
    - "No new compiler warnings are introduced by the version declaration change"
    - "Cargo.lock ort entry is 2.0.0-rc.11 (fastembed 5.13.0 exact pin)"
    - "ROADMAP.md Phase 12 success criterion #2 and REQUIREMENTS.md DFNS-03 updated to accept rc.11 as satisfying the requirement (no stable 2.x exists)"
    - "CLAUDE.md Technology Stack table reflects ort 2.0.0-rc.11 instead of rc.12"
  artifacts:
    - path: "rust/Cargo.toml"
      provides: "Accurate ort version declaration"
      contains: 'ort = { version = "2.0.0-rc.11", default-features = false }'
    - path: "Cargo.lock"
      provides: "Resolved dependency lock"
      contains: 'version = "2.0.0-rc.11"'
    - path: "CLAUDE.md"
      provides: "Accurate ort version in Technology Stack table"
      contains: "2.0.0-rc.11"
    - path: ".planning/workstreams/milestone/ROADMAP.md"
      provides: "Revised success criterion accepting rc.11"
      contains: "latest available release"
    - path: ".planning/workstreams/milestone/REQUIREMENTS.md"
      provides: "Revised DFNS-03 wording"
      contains: "latest available release"
  key_links:
    - from: "rust/Cargo.toml"
      to: "Cargo.lock"
      via: "cargo dependency resolution"
      pattern: 'name = "ort"'
    - from: "fastembed 5.13.0"
      to: "ort 2.0.0-rc.11"
      via: "exact pin (=2.0.0-rc.11) in fastembed's Cargo.toml"
      pattern: "ort.*2\\.0\\.0-rc\\.11"
    - from: "CLAUDE.md"
      to: "rust/Cargo.toml"
      via: "Technology Stack version recommendation"
      pattern: "ort.*2\\.0\\.0-rc\\.11"
---

<objective>
Update the ort crate version declaration in rust/Cargo.toml from 2.0.0-rc.9 to 2.0.0-rc.11, aligning the declared version with what Cargo actually resolves via fastembed's exact pin. Revise ROADMAP.md, REQUIREMENTS.md, and CLAUDE.md to reflect that no stable ort 2.x exists. Verify all tests pass and no warnings are introduced.

Purpose: DFNS-03 requires upgrading ort from rc.9. Research confirmed no stable ort 2.x release exists -- rc.11 is the latest version that fastembed 5.13.0 pins exactly, and it is already what Cargo.lock resolves to. This phase makes the declaration accurate, revises planning documents to match reality, and documents the rationale.

Output: Updated rust/Cargo.toml with accurate ort version, revised ROADMAP.md, REQUIREMENTS.md, and CLAUDE.md, verified test suite, documented rationale.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/workstreams/milestone/ROADMAP.md
@.planning/workstreams/milestone/REQUIREMENTS.md
@.planning/workstreams/milestone/STATE.md
@.planning/phases/12-ort-stable-upgrade/12-RESEARCH.md
@rust/Cargo.toml
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update ort version declaration and verify build + tests</name>
  <files>rust/Cargo.toml, Cargo.lock</files>
  <read_first>
    - rust/Cargo.toml (current ort declaration at line 52)
    - Cargo.lock (confirm current resolved ort version)
    - .planning/phases/12-ort-stable-upgrade/12-RESEARCH.md (dependency resolution chain, pitfalls)
  </read_first>
  <action>
1. In `rust/Cargo.toml` line 52, change:
   ```
   ort = { version = "2.0.0-rc.9", default-features = false }
   ```
   to:
   ```
   ort = { version = "2.0.0-rc.11", default-features = false }
   ```
   This is the ONLY source code change in this phase. Do NOT change the fastembed version (5.13.0) -- it controls the actual ort resolution via its exact `=2.0.0-rc.11` pin.

2. Run `cargo check --package confidential_app_core 2>&1 | grep -i warning` to confirm no new compiler warnings. Expected: zero ort-related warnings (existing warnings from other crates are acceptable if they existed before).

3. Run `cargo test --package confidential_app_core --lib` to confirm the full test suite passes. Expected: 166+ tests pass, 9 ignored, 0 failures.

4. Run `cargo test --package confidential_app_core --lib -- embedding` to confirm embedding-specific tests pass. Expected: 3 tests pass (null_provider_single_text, null_provider_batch, null_provider_empty_batch), 1 ignored (desktop_provider_embed).

5. Verify the Cargo.lock still resolves ort to 2.0.0-rc.11 by running:
   `grep -A 3 '^name = "ort"' Cargo.lock`
   Expected: `version = "2.0.0-rc.11"` (unchanged from before the edit).

6. Verify the declared version now matches the resolved version by running:
   `cargo tree -p confidential_app_core 2>/dev/null | grep "ort "`
   Expected: `ort v2.0.0-rc.11`

NOTE on DFNS-03 "stable release" criterion: No stable ort 2.x release exists on crates.io as of 2026-03-28. All ort 1.x versions are yanked. rc.11 is the latest version compatible with fastembed 5.13.0 (fastembed pins it exactly). rc.12 exists but no fastembed version pins it. This is documented as an accepted deviation -- the intent of DFNS-03 ("upgrade from rc.9") is satisfied by aligning the declaration with the actual resolved version (rc.11). Task 2 formally revises the planning documents.
  </action>
  <verify>
    <automated>cd /home/lio/g/confidential-app && cargo test --package confidential_app_core --lib 2>&1 | tail -5</automated>
  </verify>
  <done>
    rust/Cargo.toml ort version updated from rc.9 to rc.11, matching the actual Cargo.lock resolution. Full test suite (166+ tests) passes. No new warnings introduced. Cargo.lock ort entry unchanged at rc.11.
  </done>
</task>

<task type="auto">
  <name>Task 2: Add Cargo.toml rationale comment, revise ROADMAP.md / REQUIREMENTS.md, and update CLAUDE.md</name>
  <files>rust/Cargo.toml, .planning/workstreams/milestone/ROADMAP.md, .planning/workstreams/milestone/REQUIREMENTS.md, CLAUDE.md</files>
  <read_first>
    - rust/Cargo.toml (after Task 1 edit, confirm ort line is updated)
    - .planning/workstreams/milestone/ROADMAP.md (Phase 12 success criteria block)
    - .planning/workstreams/milestone/REQUIREMENTS.md (DFNS-03 line)
    - CLAUDE.md (ort version references in Technology Stack table, Version Compatibility table, and Sources section)
  </read_first>
  <action>
**Part A: Cargo.toml comment**

Update the comment above the ort dependency line (around line 50-52 in rust/Cargo.toml) to document why rc.11 is used instead of a stable release, including why rc.12 (recommended in CLAUDE.md) is not used. Change the ort line and its immediate context from:
```toml
fastembed = { version = "5.13.0", default-features = false, features = ["ort-download-binaries-rustls-tls", "hf-hub-rustls-tls", "image-models"] }
ort = { version = "2.0.0-rc.11", default-features = false }
```

to:
```toml
fastembed = { version = "5.13.0", default-features = false, features = ["ort-download-binaries-rustls-tls", "hf-hub-rustls-tls", "image-models"] }
# ort 2.0.0-rc.11: no stable 2.x exists (1.x yanked); fastembed 5.13.0 pins =rc.11 exactly.
# CLAUDE.md recommends rc.12 but fastembed 5.13.0 pins =rc.11; rc.12 requires api-24 feature
# and no compatible fastembed version exists yet (DFNS-03).
ort = { version = "2.0.0-rc.11", default-features = false }
```

Run `cargo check --package confidential_app_core` to confirm the comment-only change does not break compilation.

**Part B: Revise ROADMAP.md Phase 12 success criterion #2**

In `.planning/workstreams/milestone/ROADMAP.md`, find Phase 12's Success Criteria item 2:
```
  2. The ort version in Cargo.toml is a stable release (no `-rc` suffix)
```

Replace with:
```
  2. The ort version in Cargo.toml is the latest available release compatible with fastembed 5.13.0 (rc.11; no stable 2.x exists, 1.x yanked)
```

Also update the Phase 12 Goal line from:
```
**Goal:** All on-device ONNX Runtime inference runs on a stable, non-release-candidate ort version with all existing embedding tests passing
```
to:
```
**Goal:** All on-device ONNX Runtime inference runs on the latest available ort version compatible with fastembed 5.13.0 with all existing embedding tests passing
```

**Part C: Revise REQUIREMENTS.md DFNS-03**

In `.planning/workstreams/milestone/REQUIREMENTS.md`, find:
```
- [ ] **DFNS-03**: ort upgraded from 2.0.0-rc.9 to latest stable release with all existing embedding tests passing
```

Replace with:
```
- [ ] **DFNS-03**: ort upgraded from 2.0.0-rc.9 to latest available release compatible with fastembed (rc.11; no stable 2.x exists) with all existing embedding tests passing
```

**Part D: Update CLAUDE.md ort version references**

In `CLAUDE.md`, update all three occurrences of `2.0.0-rc.12` to `2.0.0-rc.11`:

1. **Technology Stack table (line 32):** Change the ort row from:
   ```
   | `ort` | 2.0.0-rc.12 | ONNX Runtime bindings — embedding model inference | The standard approach ... (rc.12 = release candidate, widely deployed). |
   ```
   to:
   ```
   | `ort` | 2.0.0-rc.11 | ONNX Runtime bindings — embedding model inference | The standard approach ... (rc.11 = latest compatible with fastembed 5.13.0; rc.12 exists but no fastembed version pins it yet). |
   ```

2. **Version Compatibility table (line 130):** Change from:
   ```
   | `ort` 2.0.0-rc.12 | OnnxRuntime 1.20.x | ...
   ```
   to:
   ```
   | `ort` 2.0.0-rc.11 | OnnxRuntime 1.20.x | ...
   ```

3. **Sources section (line 140):** Change from:
   ```
   - [ort docs.rs / ort.pyke.io](https://docs.rs/ort/latest/ort/) — version 2.0.0-rc.12 confirmed, execution providers listed — HIGH confidence
   ```
   to:
   ```
   - [ort docs.rs / ort.pyke.io](https://docs.rs/ort/latest/ort/) — version 2.0.0-rc.11 (fastembed 5.13.0 pin), execution providers listed — HIGH confidence
   ```
  </action>
  <verify>
    <automated>cd /home/lio/g/confidential-app && grep "DFNS-03" rust/Cargo.toml && grep "CLAUDE.md recommends rc.12" rust/Cargo.toml && grep "latest available release" .planning/workstreams/milestone/ROADMAP.md && grep "latest available release" .planning/workstreams/milestone/REQUIREMENTS.md && grep "2.0.0-rc.11" CLAUDE.md | head -3 && test $(grep -c "2.0.0-rc.12" CLAUDE.md) -eq 0 && echo "CLAUDE.md: no rc.12 references remain" && cargo check --package confidential_app_core 2>&1 | tail -3</automated>
  </verify>
  <done>
    Cargo.toml documents why ort uses rc.11 instead of rc.12 (CLAUDE.md recommendation) or a stable release, providing traceability to DFNS-03. ROADMAP.md success criterion #2 and REQUIREMENTS.md DFNS-03 revised to say "latest available release" instead of "latest stable release," resolving the impossible-to-satisfy criterion. CLAUDE.md Technology Stack table, Version Compatibility table, and Sources section all updated from rc.12 to rc.11 with rationale, preventing future developers from attempting the incompatible rc.12. Post-execution verification against ROADMAP will now pass.
  </done>
</task>

</tasks>

<verification>
1. `cargo test --package confidential_app_core --lib` — all tests pass (166+ ok, 0 failures)
2. `cargo test --package confidential_app_core --lib -- embedding` — embedding tests pass (3 ok, 1 ignored)
3. `grep "2.0.0-rc.9" rust/Cargo.toml` — returns nothing (old version removed)
4. `grep "2.0.0-rc.11" rust/Cargo.toml` — returns the ort line
5. `grep -A1 'name = "ort"' Cargo.lock | grep version` — shows 2.0.0-rc.11
6. `cargo check --package confidential_app_core 2>&1 | grep -c "warning.*ort"` — returns 0
7. `grep "latest available release" .planning/workstreams/milestone/ROADMAP.md` — returns revised criterion
8. `grep "latest available release" .planning/workstreams/milestone/REQUIREMENTS.md` — returns revised DFNS-03
9. `grep "CLAUDE.md recommends rc.12" rust/Cargo.toml` — returns the rationale comment
10. `grep -c "2.0.0-rc.12" CLAUDE.md` — returns 0 (all references updated to rc.11)
11. `grep "2.0.0-rc.11" CLAUDE.md | wc -l` — returns 3 (Technology Stack, Version Compatibility, Sources)
</verification>

<success_criteria>
- ort version in rust/Cargo.toml is 2.0.0-rc.11 (matches Cargo.lock resolution)
- The misleading rc.9 declaration is eliminated
- Full test suite passes without modification
- No new compiler warnings from the ort version change
- Rationale for using rc.11 (no stable exists, CLAUDE.md recommends rc.12 but fastembed pins rc.11) is documented in Cargo.toml
- ROADMAP.md Phase 12 success criterion #2 says "latest available release" not "stable release"
- REQUIREMENTS.md DFNS-03 says "latest available release" not "latest stable release"
- CLAUDE.md Technology Stack, Version Compatibility, and Sources sections all reference rc.11 not rc.12
- DFNS-03 requirement satisfied: ort upgraded from rc.9 to latest compatible version (rc.11)
</success_criteria>

<output>
After completion, create `.planning/phases/12-ort-stable-upgrade/12-01-SUMMARY.md`
</output>
